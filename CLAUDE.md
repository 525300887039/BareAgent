# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目概览

BareAgent 是一个纯 Python 终端代码智能体，支持可插拔 LLM 提供商、细粒度权限控制、多智能体协调和可扩展技能系统。基于 Python 3.12+，使用 Hatchling 构建。

## 常用命令

```bash
# 安装（可编辑模式）
uv pip install -e ".[dev]"
# 可选追踪后端
uv pip install -e ".[langfuse]"        # Langfuse
uv pip install -e ".[otel]"            # OpenTelemetry
uv pip install -e ".[all-tracing]"     # 全部

# 运行
bareagent                          # 或: python -m bareagent.main
bareagent --provider anthropic --model claude-sonnet-4-20250514
bareagent --config ~/my_config.toml

# 测试
pytest                             # 全部测试
pytest tests/test_loop.py          # 单个文件
pytest tests/test_loop.py -k "test_name"  # 单个测试

# 代码检查与格式化
ruff check src tests               # 检查
ruff check --fix src tests          # 自动修复
ruff format src tests               # 格式化
```

## 打包与发布 (task 06-14-pypi-tag-ci)

**src-layout**：源码在 `src/bareagent/`，**导入包名 `bareagent`**（历史上是裸 `src`，已整体迁移；下文「架构」小节里的 `src/xxx` 路径现实际为 `src/bareagent/xxx`）。**PyPI 分发名是 `bareagent-cli`**（裸 `bareagent` 被 PyPI 防仿冒判定拦截——已存在 `bare-agent`/`bare_agent`），但**命令名仍是 `bareagent`**（`[project.scripts] bareagent = "bareagent.main:main"`）。三者独立：`pip install bareagent-cli` → `import bareagent` → 敲 `bareagent`。`__init__.py` 用 `importlib.metadata.version("bareagent-cli")`（**按分发名**）取 `__version__`，失败回退 `0.0.0+unknown`。

**包内数据 + 资源定位**：`config.toml` 与 `skills/` 移入 `src/bareagent/` 包内（hatchling 对包内非 `.py` 文件自动打包，已删除旧 `force-include`）。运行时定位集中在 `src/bareagent/core/config_paths.py`（无 `main` 依赖，避免循环导入）走 `importlib.resources`：`bundled_config_path()`→`DEFAULT_CONFIG_PATH`、`local_config_path(config_path)`——**bundled 默认配置（只读、在包内）时本地覆盖 `config.local.toml` 落在「当前工作目录」(CWD)**，显式 `--config`/`BAREAGENT_CONFIG` 时仍用 `.local` 兄弟文件；`main.py` 的 `_read_config_file`/`_config_mtimes` + `provider/setup.py` 的 `init` 三处共用此函数（保证 init 写入与 load 读取同一位置）。`skills.py:resolve_skills_dir` 同理定位包内 `skills/`（env `BAREAGENT_SKILLS_DIR` 优先）。`config.local.toml` 不入包（gitignore + 在包外），无密钥泄露。

**版本 = hatch-vcs 从 git tag 派生**：`[build-system]` 加 `hatch-vcs`，`[project] dynamic=["version"]`，`[tool.hatch.version] source="vcs"` + `fallback-version="0.0.0"`（无 git/tag 时不崩）+ `tag-pattern`（只认 `v*`，规避杂牌 tag `backup-before-email-rewrite`）+ `raw-options.local_scheme="no-local-version"`（让未打 tag 的 dev 构建产出无 `+local` 段、可上传 TestPyPI 的版本如 `0.1.1.devN`）。打 `vX.Y.Z` tag = 定版本，**不手改 pyproject**；dirty/无 tag 工作树会得到 dev 版本（正常）。

**CI 自动发布**：`.github/workflows/release.yml` —— 推 `v*` tag → 正式 **PyPI**；手动 `workflow_dispatch` → **TestPyPI** 演练。PyPI **Trusted Publishing (OIDC) 无 token**：`id-token: write` 仅加在 publish job，checkout 必须 `fetch-depth: 0`（否则 hatch-vcs 看不到 tag），build job 内置 `twine check`，环境 `pypi`/`testpypi`（建议给 `pypi` 加 required reviewer）。**首发前需用户手动**在 pypi.org + test.pypi.org 各登记一次 pending publisher（owner=`525300887039` / repo=`BareAgent` / workflow=`release.yml`）+ 建 GitHub Environments——完整步骤与排错见 `docs/releasing.md`。

## CI 可见性 (task 06-20-ci-pre-push-main, 06-20-ci-manual-linux-job-socket-http, 06-21-ci-windows-latest-job, 06-21-pyright-ci)

防止 main CI 再次长期变红无人察觉（真实事件：CI 从 ~2026-06-14 红到 06-20，根因是 `from tests.conftest import` 在裸 `uv run pytest` 下 `ModuleNotFoundError`，已用 `pyproject [tool.pytest.ini_options] pythonpath=["."]` 修好——见 commit `2a8fbee`）。**元问题**：本机习惯 `python -m pytest`（前插 cwd 到 sys.path）掩盖了 CI 裸 `uv run pytest` 才暴露的导入差异，且无「push 前」/「main 变红时」提醒。决策**不强制 PR / branch protection**（单人仓库、大量 chore 直接推 main 是刻意低摩擦工作流），靠两道互补防线兜：

**(1) push 前本地闸**：`scripts/ci-check.sh`（bash，地基，被 hook 与手动复用）跑 CI 同款 `uv run ruff check src tests` + `uv run pytest`——**铁律：用 `uv run` 而非 `python -m pytest`**（后者前插 cwd 掩盖 sys.path 差异，正是元凶）；`uv` 缺失 fail-closed（exit 127）。`.githooks/pre-push` 调脚本拦失败，跳过旋钮 `BAREAGENT_PREPUSH_SKIP=1` + git 原生 `--no-verify`；`scripts/setup-hooks.sh` 一次性 `git config core.hooksPath .githooks`（committed hook 不自动生效，每 clone 装一次）。

**(2) main 变红服务端兜底**：`.github/workflows/ci.yml` 加 `notify` job（`needs: [test, socket]`，`if: always() && github.event_name=='push' && refs/heads/main`，`permissions: issues: write`）调 `scripts/ci_notify.py`。决策逻辑抽成**纯函数 `decide_action(open_issue_count, conclusion) -> Action`**（CREATE/COMMENT/CLOSE/NOOP，注入式可单测），thin `main` 经 `gh` CLI 落地：main 失败→无 open issue 则开/有则评论，成功→有则评论恢复+关闭，cancelled/skipped→NOOP；去重靠固定 label `ci-failure`（幂等建）、标题纯文字无 emoji。**多 job 归并**：notify run 步骤传 `test:<result>` + `socket:<result>` 两个 `--conclusion`，纯函数 `combine_conclusions(results) -> str`（任一 failure→failure / 全 success→success / 否则 ""=NOOP）先归并再喂 decide_action；issue 正文带是哪个 job 红的。

**(3) socket 测试上 CI（task 06-20-ci-manual-linux-job-socket-http）**：web_viewer + MCP http transport 的 ~11 个 localhost-socket 测试此前零自动化覆盖（本机 loopback flake 标 `manual` 永久跳过、CI 默认 `-m 'not manual'` 也排除），但它们在 Linux 上稳跑。`tests/conftest.py:pytest_collection_modifyitems` 给「`test_web_viewer`(按路径) + 用 socket fixture(json/sse/legacy_server)」两支**额外**打 `pytest.mark.socket`（`*_manual.py` 不打，零重叠——后者需 API key/外部 server 不能进 CI），pyproject `markers` 登记 `socket`；ci.yml 加**阻塞** `socket` job 跑 `uv run pytest -m socket`（`-m socket` 在 CLI 覆盖 addopts 的 `-m 'not manual'`，故选中这批虽带 manual 仍跑）。本机默认 `pytest` 行为字节级不变（这批仍带 manual、默认排除）。决策：阻塞（非阻塞=覆盖剧场）+ 分支 PR 先验证连续绿再 ff-merge main；纳入 notify 的 needs（阻塞 job 失败=main 红，否则留通知盲点）。

**(4) windows-latest 上 CI（task 06-21-ci-windows-latest-job）**：开发主力是 Windows、CI 此前只在 ubuntu 跑（导致 main 红一周的根因正属平台差异类）。`test` job 改 `strategy: {fail-fast: false, matrix: {os: [ubuntu-latest, windows-latest]}}` + `runs-on: ${{ matrix.os }}`，在 CI 也覆盖 Windows；Lint(ruff) 步骤加 `if: runner.os == 'Linux'`（跨平台结果一致只跑一次）。`notify` 的 `needs: [test, socket]` **不改**——matrix job 的 `needs.test.result` 是**聚合**值（任一 OS leg 失败即 failure），windows 失败自动并入通知，零额外接线。`socket` job 维持仅 ubuntu（socket 测试在 Windows loopback flake）。`fail-fast: false` 让一个 OS 失败不取消另一个的结果。实测：套件在 windows-latest 干净 runner 绿（1218 passed / 10 skipped；ubuntu 1216 passed / 12 skipped，平台 `skipif` 差异属正常，selected 都 1228），PR 连续两次绿后 ff-merge。决策：阻塞为目标 + 分支验证 + triage（真 bug 修 / 平台特异 skipif / 失控退 `continue-on-error`）；本次零 triage 一次过。

**(5) pyright 类型检查上 CI（task 06-21-pyright-ci）**：`pyproject [tool.pyright]` 早配好（basic / include=src/bareagent / reportMissingImports=warning）却从未在 CI/push 执行——又一例「检查存在但没在该执行处执行」（同 main 红一周的元模式）。`test` job 加 Linux-gated step「Type check (pyright)」跑 `uv run pyright`（类型检查平台无关，同 ruff 只在 Linux 跑一次；**阻塞**——error 致 test job 红→自动并入聚合 `needs.test.result`→notify 已覆盖，零额外接线）；`scripts/ci-check.sh` 同步加 `uv run pyright`（变 4 步：ruff check→format --check→pyright→pytest，本地闸与 CI 忠实一致）。**exact pin `pyright==1.1.409`**（同 ruff：PyPI 包是 node pyright 的 wrapper、默认下匹配版本不自动升级，pin 它即 pin 检查器；新版可能涌新 error，升级同 PR re-triage）。**triage**：探查报 10 errors / 7 warnings（warnings 全是未装可选依赖的 `reportMissingImports`，warning 级不致红——pyright exit code 只由 error 决定），10 个 error 全部**真修复**（无 `# pyright: ignore`、无放松 `reportXxx` 级别）：`main.py` budget 解析改内联 isinstance narrowing（×2，narrowing 别藏单独布尔里 pyright 不跨变量传递）、`code_index.py`/`persistent.py` 的 `_search`/`_semantic_recall` 各加 `assert self._embedder is not None`（调用方 `search`/`recall` 已守卫 None，assert 仅作跨方法 narrowing + 不变量文档，×6）、`repo_map.py` pagerank `if total_p>0` 分支加 `assert personalization is not None`、`repo_map_extract.py` tree-sitter `Node.text or b""` 兜底（`Node.text` 是 `bytes|None`）。**环境差异要点**：CI 只装 `.[dev]`（无 repo-map/embeddings/lsp/tracing extra）→ 未解析的可选 import 类型为 Unknown→抑制而非制造 error，故 **CI error 集 ⊆ 本机集**（本机装了 tree-sitter 是更严格环境，`repo_map_extract` 那条 error CI 可能不出现）；修完本机 10 个 ⇒ CI 必 ≤ 0。决策：阻塞为目标 + 分支 PR 验证连续绿再 ff-merge（同 windows-latest 流程）+ triage（真修复优先 / 失控退 `continue-on-error` 保 main）；维持 basic（不收紧 standard）、include 不扩 tests、不单开 job、不在 windows leg 跑。

**跨平台硬化**：`.gitattributes` **窄范围**钉 `*.sh` + `.githooks/pre-push` 为 `eol=lf`（本机 `core.autocrlf=true` 会把 shell 脚本工作区转 CRLF，Linux CI 的 bash 遇 `\r` 破 shebang/命令）；**不**做全树 `text=auto` 以免 renormalize churn。脚本/hook 入库为 `100755`（Windows `core.filemode=false`，靠 `git update-index --chmod=+x`）。

**防回归 guard**：`tests/test_ci_visibility.py` = `decide_action` + `combine_conclusions` 参数化决策表单测 + 静态断言（ci.yml/ci-check.sh 用 `uv run pytest` 非 `python -m pytest`（只看非注释行，保留解释注释）、ci.yml 有 notify job + `issues: write`、有 `socket` job 跑 `-m socket`、notify `needs: [test, socket]`、`test` matrix 含 `windows-latest`+`ubuntu-latest`+`fail-fast: false`、pre-push hook 接线在位、pyproject 保留 `pythonpath=["."]` + 登记 `socket` marker、ci.yml + ci-check.sh 含 `uv run pyright`、pyright exact pin（无 `pyright>=`））——把「CI 别退回掩盖态 / socket 覆盖别丢 / windows 覆盖别丢 / pyright 门别丢」这些教训机制化固化。MVP 不做：branch protection、Slack/邮件通知、PR（非 main）失败通知、pre-commit / PowerShell hook、`*_manual.py` 中需 key/外部服务的测试上 CI、多 Python 版本/macOS 矩阵、notify 失败路径真实冒烟、`workflow_run` 跨 workflow 解耦、pyright 收紧 standard/strict、pyright include 扩到 tests、pyright 单开 job / 在 windows leg 跑（均后续扩展位）。关键文件：`scripts/{ci-check.sh,setup-hooks.sh,ci_notify.py}`、`.githooks/pre-push`、`.github/workflows/ci.yml`（test(matrix ubuntu+windows, 含 pyright step)/socket/notify）、`pyproject.toml`（`[tool.pyright]` + ruff/pyright exact pin）、`tests/conftest.py`（socket marker 分流）、`.gitattributes`、`tests/test_ci_visibility.py`。README「本地 CI 闸」小节有安装/手动跑/跳过说明。

## 架构

### 核心智能体循环 (`src/core/loop.py`)
`agent_loop()` 是中央调度器：调用 LLM → 解析工具调用 → 权限检查 → 执行处理器 → 收集结果。最多迭代 `max_iterations`（200）次。支持流式输出和长对话消息压缩。

### LLM 重试策略 (`src/core/retry.py`，ROADMAP 4.2)
瞬时性 LLM 调用失败（rate limit / 网络超时 / 5xx / overloaded）自动指数退避重试，不可重试错误（认证 / 400 bad request / 模型不存在 / 未知异常）立即上抛——不掩盖真正的配置错误。`retry.py` 是**纯模块**（无 LLM/loop/SDK 依赖，注入 `sleep`/`rng` 可单测）：`RetryPolicy`（enabled/max_attempts/base_delay_sec/max_delay_sec/multiplier/jitter）+ `is_retryable`（**provider 无关** duck-typing：先看 `status_code`/`status` 属性，再按异常类名匹配连接/超时类——**不 import anthropic/openai**；可重试 408/409/429/500/502/503/504/529 + 其余 5xx 兜底 + 连接/超时类名，不可重试 400/401/403/404/413/422 + 未知异常保守 fail-fast；非 Exception 如 KeyboardInterrupt 永不重试）+ `compute_delay`（`min(max_delay, base_delay × multiplier^(attempt-1))` + 可选 full jitter `uniform(0, delay)`）+ `run_with_retry`（驱动：耗尽后上抛**最后一次**原异常，`on_retry` 回调供 observability）。**驱动点**：`loop.py:_invoke_provider` 把整次 provider 调用（含流式消费，D5）包进 `run_with_retry`——重试包整层而非 mid-stream 续传，故流式重试会**重启 StreamPrinter 重打印已打印的局部文本**（罕见，transient 错误绝大多数在首 token 前，已知限制）。`agent_loop` 加可选 `retry_policy` 参数（默认 None = 旧直抛行为，向后兼容）；重试耗尽后原异常仍落到 `except BaseException` → `raise LLMCallError(...) from exc`（契约不变）。`_StreamingUnavailableError`/`NotImplementedError` 流式回退是控制流信号、无 status_code、类名不在白名单 → `is_retryable` 天然返回 False，不被重试干扰。**app 层独占重试**：`AnthropicProvider`/`OpenAIProvider` 构造 client 时 `max_retries=0` 关掉 SDK 自带重试，避免 2×N 复合放大（`enabled=false` 即真正无重试，含 SDK）。**子代理继承（D6）**：`retry_policy` 透传 `get_handlers(subagent_retry_policy=)` → subagent lambda → `run_subagent` → `_run_subagent_sync` → 子 `agent_loop`（含后台子代理 + 嵌套子代理），让后台子代理也扛瞬时失败（注意 `hook_engine` 子代理**不**传，retry_policy 传）。配置见 `config.toml [retry]`（`_parse_retry_config` 逐字段容错不崩 boot，`enabled`/`max_attempts` 走 env 覆盖 `BAREAGENT_RETRY_ENABLED`/`BAREAGENT_RETRY_MAX_ATTEMPTS`，其余 config-only；`_build_retry_policy` 做 `RetryConfig`→`RetryPolicy` 转换）。关键文件：`src/core/retry.py`、`src/core/loop.py`（`_invoke_provider` + `agent_loop` 参数）、`src/provider/{anthropic,openai}.py`（`max_retries=0`）、`src/main.py`（`RetryConfig` + 解析 + 两处调用点注入）、`src/planning/subagent.py`（透传）。MVP 不解析 Retry-After、不做流式 mid-stream 续传/去重、不做熔断/令牌桶/跨会话重试预算（均为后续扩展位）。

### 提供商抽象 (`src/provider/`)
`BaseLLMProvider`（base.py）为抽象基类，`AnthropicProvider` 和 `OpenAIProvider` 为具体实现（OpenAI provider 也覆盖 DeepSeek / Qwen / GLM / **Gemini** 等 OpenAI 兼容端点——provider 经 `presets.py` 的静态 `ProviderPreset` dict 注册，`gemini` preset route=openai、base_url 指向 `generativelanguage.googleapis.com/v1beta/openai/`、key env `GEMINI_API_KEY`、缓存读走标准 `cached_tokens` 字段无需专属分支）。`factory.py` 负责工厂创建。统一的 `LLMResponse` 包含工具调用、文本、思考过程、token 计数。支持流式（`create_stream()`）和非流式（`create()`）。**缓存能力枚举**：`BaseLLMProvider.cache_mode: ClassVar`（`base.py`）= `explicit`（Anthropic 显式断点）/ `auto`（OpenAI·DeepSeek·Gemini 自动前缀缓存、无请求侧旋钮、只读用量）/ `none`（默认）——provider 级事实，`/cost` 在有缓存活动时据此标注当前 provider 缓存模式；`token_tracker` **不**消费它（只见 model 串、不做 model→provider 反查）。

### Prompt 缓存 (`src/provider/`，ROADMAP 提效降本)
给反复重发的大块上下文打缓存断点，让多轮 agent loop 的输入成本走缓存读（约 0.1× 输入价）。`CacheConfig`（`base.py`，`enabled`/`ttl`，复刻 `ThinkingConfig` 的 config→dataclass→factory→provider 穿透范式）。**断点注入仅在 `AnthropicProvider._build_request_params`**（loop/provider 接口零改动）：缓存开启时给最后一个 tool 挂 `cache_control`、把 `system` 由 bare string 转成含 `cache_control` 的 text block 列表（Anthropic 要求 system 为 block 列表才能挂断点）、并由 `_apply_conversation_breakpoint` 挂**两个**对话断点：(1) 给最近一条消息的最后一个**可缓存** block（text/image/tool_use/tool_result/document，跳过 thinking）挂**移动**断点（每请求只比上次多几个 block，20-block 回溯稳命中增量缓存）；(2) 给**最近一条真实 user 轮次**（不含 tool_result 块的 user 消息，`_is_real_user_turn` 判定；tool_result 也是 user 角色故须排除）挂 **anchor** 断点——agentic 多工具爆发期间该真实 user 轮次不动，其缓存前缀字节稳定、覆盖到用户请求大头，兜住「单 turn 追加 >20 block 致移动断点 20-block 回溯够不到 → 静默全 miss」的失效场景。断点数 tools(1)+system(1)+anchor(1)+移动(1) = 4（用满 Anthropic 上限）；只有一条 user 消息（或无更早真实 user 轮次）时优雅退化为单移动断点（3 个）。`cache_control` 值 5m=`{"type":"ephemeral"}`、1h=`{"type":"ephemeral","ttl":"1h"}`。**`cache_config=None` 或 `enabled=false` ⇒ 缓存关闭、请求体与未接缓存前字节级一致**（裸 `AnthropicProvider(api_key, model)` 构造默认 None=关；`factory` 总是传 enabled 实例，故 app 默认 ON）。渲染顺序 tools→system→messages，BareAgent 的 system 会话内静态（memory recall 注入 user 消息末尾不污染前缀），天然适合缓存；最小可缓存前缀**随模型而异**（Opus 4.5/4.6 + Haiku 4.5 = 4096，Opus 4.8/Sonnet 4.6 = 1024，按目标模型查实时 API 表，两官方源对新模型尚有出入），低于阈值静默 no-op（不报错；代码不做客户端阈值闸，纯靠 Anthropic 静默兜底）。**用量归一化（跨 provider）**：`LLMResponse` 加 `cache_creation_input_tokens`/`cache_read_input_tokens`，三字段语义统一为 `input_tokens=全价` / `cache_read=折扣读` / `cache_creation=写溢价`（**相加非重叠**）；各 provider 的 `_parse_response` 负责归一——Anthropic 直接透传 usage（流式从 `get_final_message().usage` 读），OpenAI `input=prompt_tokens-cached`、`cache_read=prompt_tokens_details.cached_tokens`，DeepSeek `cache_read=prompt_cache_hit_tokens`（`_extract_cached_tokens` 统一抽取，含 chat/responses/两条流式路径 + `_merge_streamed_responses_result` 保留）。OpenAI provider **不收** `cache_config`（自动缓存无旋钮，只读用量）。**计价**：`token_tracker.py` 的 `CacheEconomics`（frozen+slots：`read_mult`/`write_mult`/`min_cacheable_tokens`/`controllable_ttl`，**取代**旧 2-tuple `DEFAULT_CACHE_MULTIPLIERS`）+ `DEFAULT_CACHE_ECONOMICS`（家族前缀→economics，`resolve_cache_economics` 复用 `_longest_prefix_match`）= claude(0.1,1.25,min1024,ttl可控)/gpt·o1·o3·o4(0.5,0,min1024)/**gpt-5(0.1,0,min1024)**（更长前缀压过 `gpt`，GPT-5 缓存读九折而非五折）/deepseek(0.1,0)/**gemini(0.1,0,min2048)**，未知家族回退 (0.1,1.25)（折扣随 provider 版本漂移，权威来源是响应上的归一化用量字段，这些倍率只喂 `/cost` 估算；`min_cacheable_tokens`/`controllable_ttl` 目前为描述性元数据，尚未强制）；`resolve_cache_multipliers` 保留为返回 `(read,write)` 的薄 wrapper（向后兼容旧调用/测试）。`estimate_cost`/`summary` 计入缓存读写，`/cost` 仅在有缓存活动时多显 `Cache: N read / M write` 行 + 一行当前 provider `Cache mode:`（无缓存时输出与原先一致）。配置 `[cache]`（`enabled` 默认 ON + env `BAREAGENT_CACHE_ENABLED`、`ttl` `5m`/`1h`，`_parse_cache_config` 逐字段容错），boot 固化随 provider 走配置热重载 **restart-required**（不进 hot 集）。MVP 不做 1h 写溢价精确 5m/1h 拆分计价（统一按 1.25× 近似）、不做更激进断点策略（动态阈值/thinking 缓存/per-tool 断点）、不做 bottom-toolbar 实时命中率、不做缓存开关热重载、不做 `cache_key` 透传（OpenAI `prompt_cache_key` 路由黏性，投入产出倒挂，缓做扩展位——自动缓存本就按前缀命中故无功能损失）、不做 Gemini 实测缓存命中验证（无 key）/Gemini 原生 cachedContent 显式缓存。关键文件：`src/provider/base.py`（`CacheConfig` + `LLMResponse` 缓存字段 + `cache_mode` ClassVar）、`src/provider/anthropic.py`（断点注入 + usage 解析 + `cache_mode="explicit"`）、`src/provider/openai.py`（`_extract_cached_tokens` 归一化 + `cache_mode="auto"`）、`src/provider/presets.py`（`gemini` preset）、`src/memory/token_tracker.py`（`CacheEconomics`/`DEFAULT_CACHE_ECONOMICS` + 计价/展示）、`src/provider/factory.py`（`_build_cache_config` 穿透）、`src/main.py`（`[cache]` 解析 + Config 字段 + teammate factory 穿透 + `/cost` 缓存模式行）。配置见 `config.toml [cache]`。

### 工具系统 (`src/core/tools.py`)
工具以可调用对象注册在字典中。基础工具（`BASE_TOOLS`）：bash、read_file、write_file、edit_file、glob、grep、web_fetch、web_search。`grep` 支持 `output_mode`（对标 ripgrep / Claude Code）：`content`（默认，`file:line:text` 每行）/ `files_with_matches`（仅去重文件路径，最省 token）/ `count`（`file:N` 每文件匹配数）；非法值优雅退化为 `content`。延迟加载工具（`DEFERRED_TOOLS`）：todo_*、task_*、subagent、load_skill、background_run、team_*。Schema 定义在 `core/schema.py`，处理器在 `core/handlers/`（含 `web_fetch.py`、`web_search.py`、`search_utils.py`）。`read_file` 支持多模态：按扩展名分派——图片（png/jpg/jpeg/gif/webp）→ base64 image 块（需 vision 模型）、PDF（.pdf）→ 按页提取文本（需 `[pdf]` extra，pypdf，lazy import，未装时返回友好提示而非崩溃，`pages` 参数选页范围如 "1-5"/"3"）、notebook（.ipynb）→ json 解析渲染 markdown/code cells + 输出、其余走 UTF-8 文本路径（offset/limit）。图片块复用 Anthropic 内部 shape，经 `loop.py:_tool_result` 的 `list[dict]` 直通通路（零改 loop/provider）。

### 权限模型 (`src/permission/guard.py`)
模式：DEFAULT（写操作需确认）、AUTO（安全模式自动批准）、PLAN（仅允许安全工具）、BYPASS（无检查）。内置危险模式检测（rm -rf、force push、DROP TABLE 等）。支持 allow/deny 规则（前缀匹配）。`clone()` 创建权限副本，`for_subagent()` 为子智能体创建隔离权限（模式级联 + fail-closed）。运行时可通过 `/default`、`/auto`、`/plan`、`/bypass`、`/mode` 命令或 `Shift+Tab` 快捷键切换权限模式。

### Plan 模式工作流 (`src/core/handlers/plan.py`，对标 Claude Code `ExitPlanMode`)
把 PLAN 从"只读权限闸"升级为 **plan → 呈递 → 审批 → execute** 闭环。PLAN 模式下每个 agent_loop 迭代经 `compact` 注入 `<plan-mode>` system 段（`main.py:_refresh_plan_directive`，仿 `_refresh_nag_reminder` 先剥旧块再按 `permission.mode==PLAN` 条件注入；指令文本 = `context.py:PLAN_MODE_DIRECTIVE`），引导 LLM 只读调研、**不改文件**、完成后调 `exit_plan_mode(plan=markdown)` 呈递计划而非盲目撞写工具被拒。`exit_plan_mode` 是**主循环专属工具**：schema/handler 在 `core/handlers/plan.py`、**不进** `get_tools()` 全局集，只在 `main.py` boot + 每次会话切换（`/new`·`/compact`·`/resume`·`/import` 重建 handlers 后）经 `_install_plan_handler` 注入主循环 `tools`/`handlers`。**子代理隔离三重保障**：(1) 不在全局集；(2) `agent_types.MAIN_LOOP_ONLY_TOOLS` 让 `filter_tools` 对**所有**子代理类型恒剥离它；(3) `filter_handlers` 丢弃孤儿 handler——任何子代理（含 general-purpose）都不能翻转父级权限模式。入 `PermissionGuard.SAFE_TOOLS`（否则 PLAN 下非 SAFE 工具被 `ask_user` 拦死、连退出 PLAN 的工具都用不了）。**handler 纯逻辑**（不 import permission/ui）：校验 plan 非空 → 调注入的 `approve_fn` → 把 `PlanDecision` 映射成回灌 LLM 的结果字符串；UI 渲染 + 模式翻转由 `main.py:_make_plan_approval` 的审批回调持有（持有 `PermissionGuard`+`AgentConsole`）。**三选一审批**（仿 `_handle_mode_selection_stdio`）：1) 批准→翻 `DEFAULT`（写仍逐个确认）2) 批准并自动执行→翻 `AUTO`（危险命令仍拦）3) 拒绝→留 `PLAN` 并追问可选理由回灌 LLM 改稿。批准后**同一 agent_loop 不中断续跑**实现，因 `compact` 每迭代调用，下一迭代自动剥离 `<plan-mode>` 指令（无残留）。**边界 fail-closed**：非 PLAN 调用→`noop`（不翻转）、非交互/无 TTY→`unavailable`（留 PLAN）、空 plan→结构化 `Error:`、审批 EOF/中断→视为 reject。MVP 不做：计划落盘/历史（`/export`+trellis prd 已承载）、子代理自动 ExitPlanMode、计划行内编辑、`[plan]` 配置段。关键文件：`src/core/handlers/plan.py`、`src/core/context.py`（`PLAN_MODE_DIRECTIVE`）、`src/main.py`（`_refresh_plan_directive`+`_make_plan_approval`+`_install_plan_handler`+`_build_loop_compact` 接 `permission`）、`src/permission/guard.py`（SAFE_TOOLS）、`src/planning/agent_types.py`（`MAIN_LOOP_ONLY_TOOLS`）。

### Goal 完成条件循环 (`src/core/goal.py`，对标 Claude Code `/goal`)
`/goal [--max-turns N] <condition>` 让 agent **自驱动一轮接一轮**工作，每轮 `agent_loop` 停止后由一个**独立评估器**判定完成条件是否满足；未达标把理由回灌为新 user 消息继续，达标或触达 `max_turns` 即停。把 BareAgent 从"一问一答 + 手动续跑"补上"自驱动到达标"这一档。**同步且不持久化**：命令期间阻塞 REPL、跑完即返回、无跨输入残留状态（故无 `/goal clear`/状态子命令），Esc/中断可终止。`src/core/goal.py` 是**纯逻辑模块**（无 LLM/loop/REPL/SDK 依赖，仿 `retry.py`/`skill_gen.py` 注入回调可单测）：`GoalState`/`Verdict`/`GoalCommand`/`GoalOutcome` + `parse_verdict`（容错——缺 `met` 字段/非 dict → `malformed`=未达标，绝不蒙对成功）+ `parse_goal_command`（解析 `--max-turns` 前缀 flag + 空条件报错）+ prompt 构造（initial/evaluator/continuation）+ `run_goal_loop`（驱动器：注入 `run_turn`/`evaluate` 两回调，返回 `(GoalOutcome, last_verdict)`；只返回 MET/MAX_TURNS，中断由调用方译成 ABORTED）。**评估器（transcript-only）**：`main.py:_run_goal_evaluator` 仿 `_run_skill_reflection`——在**消息副本**上跑隔离 `agent_loop`、唯一工具 `goal_verdict(met,reason)`（强制结构化裁决，`max_iterations=3`、`skill_gen=None`），故真实历史/turn 不被污染；**never-raise**：LLM 错误/无裁决 → 返回 `malformed` 未达标（仍受 `max_turns` 兜底），仅 `KeyboardInterrupt` 上抛以中止整个循环。`goal_verdict` schema/handler 在 `core/handlers/goal.py`、**不进** `get_tools()` 全局集（只在评估器隔离调用里暴露，子代理拿不到，同 `skill_create` 范式）；入 `PermissionGuard.SAFE_TOOLS`（纯内存记录裁决无副作用，否则 DEFAULT 下每轮弹确认）。**驱动**：`main.py:_drive_goal` 持有 `run_turn`（复用主循环 `loop_tools`/`handlers`/`permission`/`compact_fn` 跑真实 turn，`skill_gen=None` 不触发反思；中断/`LLMCallError` → `del messages[snapshot:]` 回滚在飞 turn 后上抛，已完成轮次保留）+ `evaluate`（调评估器）。**权限 fail-closed**：尊重当前模式、**绝不自动升级**（与 Plan 审批/`/loop`/hooks 一脉相承）；DEFAULT 下启动打提示引导 `/auto` 做无人值守。**评估器模型**：`[goal] evaluator_model` 留空=复用会话 provider（`_build_goal_provider` 直接返回），设值=经 `factory`（`replace(config, provider=replace(config.provider, model=...))`）单独构造便宜 sibling provider，构造失败 fail-open 回退会话 provider + warn。配置 `[goal]`（`max_turns` 默认 25 走 env `BAREAGENT_GOAL_MAX_TURNS` + 行内 `--max-turns N` 覆盖、`<1` 回退默认；`evaluator_model` config-only；`_parse_goal_config` 逐字段容错不崩 boot），boot 固化随 provider restart-required（不进热重载 hot 集）。MVP 不做：跨 `/resume` 持久化未达标目标、wall-clock 超时、评估器只读验证工具、把 goal 抽象成通用 `Stop` hook 事件、把 goal 暴露成 LLM 可调工具（均后续扩展位）。关键文件：`src/core/goal.py`、`src/core/handlers/goal.py`、`src/main.py`（`GoalConfig`+`_parse_goal_config`+`_build_goal_provider`+`_run_goal_evaluator`+`_drive_goal`+`/goal` dispatch）、`src/permission/guard.py`（SAFE_TOOLS）。配置见 `config.toml [goal]`。

### Workflow 确定性编排 (`src/core/workflow.py`，对标 Claude Code `Workflow`)
让 LLM 临场产出一张**静态声明式 DAG**（节点 = 子代理任务 + `depends_on` 边）并**确定性**执行：相互独立的节点**并发**跑、依赖就绪才跑、上游产物喂给下游、收尾结构化聚合回灌——补上"模型驱动、串行、一次次手发 `subagent`"之外的"代码描述的并行编排"这一档。对标 CC 的 `Workflow` 工具但**取其确定性 fan-out 内核**、砍掉可执行脚本运行时（用声明式 DAG 替代任意代码 `exec`，绕开沙箱/安全/可测性难题）与 resume/budget。**`workflow` 是主循环专属工具**：schema 在 `core/handlers/workflow.py`、**不进** `get_tools()` 全局集，只 append 到 `loop_tools`（非喂子代理的基础 `tools`）、handler 经 `main.py:_install_workflow_handler` 注入主循环（boot + 每次会话切换 `/new`·`/compact`·`/resume`·`/import` 重建 handlers 后重装，仿 `_install_plan_handler`）。**子代理隔离三重保障**：(1) 不在全局集；(2) `"workflow"` 入 `agent_types.MAIN_LOOP_ONLY_TOOLS` 让 `filter_tools` 对所有子代理类型恒剥离；(3) `filter_handlers` 丢孤儿——任何子代理拿不到 `workflow`（MVP 无嵌套）。`src/core/workflow.py` 是**纯逻辑模块**（零 LLM/loop/线程/SDK 依赖，仿 `goal.py`/`retry.py` 注入回调可单测）：`WorkflowNode`/`NodeStatus`/`NodeResult`/`WorkflowSpec`/`WorkflowError` + `parse_workflow`（容错解析，非 dict/无 nodes 数组抛 `WorkflowError`，单节点缺字段宽松降级）+ `validate_workflow`（id 唯一非空、prompt 非空、无自依赖、`depends_on` 引用存在、`_find_cycle` DFS 环检测、`max_nodes` 上限 → 返回错误列表，**有错不执行**）+ `compute_ready`（PENDING 且 deps 全 DONE）+ `propagate_skips`（**失败语义(b)**：任一上游 FAILED/SKIPPED ⇒ 本节点 SKIPPED，跑到 fixpoint 传递级联，独立分支不受影响）+ `build_node_prompt`（`{{node_id}}` 占位替换上游产物 + 未被占位引用的 dep 自动附 `# Upstream results` 段，未知占位原样留）+ `format_summary`（done/failed/skipped 计数 + 每节点 status/产物/错误段）+ `run_workflow`（驱动器：**分层执行**——每轮 `propagate_skips`→`compute_ready`→把整个就绪集经注入的 `map_concurrent` 并发跑→更新 results，直到无就绪节点；`execute_node`/`map_concurrent`/`on_progress` 全注入。已知 MVP 简化：节点等整层 peer 而非仅自身依赖，连续 as-completed 调度留后续）。**节点执行（`main.py:_install_workflow_handler` 闭包）**：`execute_node` 调 `build_node_prompt` 后复用 `run_subagent(provider, prompt, base_tools, node_handlers, node_permission, agent_type=node.agent_type, retry_policy=, bg_manager=)` 同步路径（不另起子代理栈）；**节点子代理 fail-closed 无人值守跑**——`permission.clone(fail_closed=True)`（worker 线程不能弹确认，与后台子代理/`/loop` 一脉相承：DEFAULT 写被拒、AUTO 自动放行、PLAN 只读），尊重当前模式**绝不自动升级**。**线程安全**：`_run_node_batch` 用 `ThreadPoolExecutor`（保序、单节点免开线程、`KeyboardInterrupt` cancel 待执行 + `shutdown(wait=False)` 后上抛 → 经 BaseException 穿出 `agent_loop` 被 REPL 接住中止）；worker 只跑静默 `run_subagent`（不传 console/messages），**所有 console 进度由 driver 经 `on_progress`→`console.print_status` 在主线程发**（仿 `Scheduler` 不在 timer 线程碰 console）；`run_subagent` 的 `filter_tools`/`filter_handlers` 返回新结构，节点间不共享可变 handler 状态。**结果回灌**：聚合 summary 作 tool_result 回 LLM 推理链（同步阻塞，无 notification 二次往返）。**配置 `[workflow]`**：`enabled` 默认 ON + env `BAREAGENT_WORKFLOW_ENABLED`（false 时 schema 不注入、install no-op，**整特性短路**）、`max_concurrency`（默认 8）、`max_nodes`（默认 20），`_parse_workflow_config` 逐字段容错不崩 boot，boot 固化随 restart-required（diff 自动归 restart，不进 hot 集）。MVP 不做：循环/条件/动态 fan-out（静态 DAG，分支靠主循环二次发 workflow）、可执行脚本运行时、后台执行/`/workflows` 面板/resume/token budget、worktree per-node 隔离（节点默认 `isolation="none"`）、嵌套 workflow、节点 wall-clock 超时/model 覆盖、schema 强制结构化输出（均后续扩展位）。关键文件：`src/core/workflow.py`、`src/core/handlers/workflow.py`、`src/main.py`（`WorkflowConfig`+`_parse_workflow_config`+`_run_node_batch`+`_install_workflow_handler`+`loop_tools` append+5 处安装）、`src/planning/agent_types.py`（`MAIN_LOOP_ONLY_TOOLS`）。配置见 `config.toml [workflow]`。

**后台执行 + /workflows 面板 + resume + token budget（task 06-08-workflow-background-panel-resume-budget）**：补齐 06-06 列为 Out of Scope 的四个扩展位，全部复用现成范式（后台子代理 daemon、`_drain_team_mailbox` 缓冲回灌、`SubagentRegistry` FIFO/clear、`TokenTracker`），纯逻辑进 `core/workflow.py`、副作用留 `main.py`。**(1) 后台执行（Q1=a+a）**：`workflow` schema 加可选 `run_in_background`（默认 false，同步路径**字节级不变**）；为 true 时 `bg_manager.submit(run_id, background_runner)` 在 daemon 线程跑整个 `run_workflow`、工具**立即返回** `Workflow <run_id> started...`。后台线程**绝不碰 console**（线程安全铁律，仿 Scheduler）——`on_progress` 在后台模式是 no-op，进度只落 registry 供面板读；同步模式 `on_progress` 仍走 `console.print_status`。完成时把**完整聚合 summary**（不走通用 500 字截断）经 `main.py:_drain_workflow_results`（镜像 `_drain_team_mailbox` + `pending_workflow_messages` 缓冲）在下个 user turn 前缀注入 LLM；`registry.take_undelivered()` 保证每个完成 run 只投递一次（`delivered` 去重，仅后台 run 注入——同步 run 已作 tool_result 返回故 drain 跳过）；`concurrency/notification.py:inject_notifications` **跳过 `wf-` 前缀** task_id（避免后台 run 既走通用截断通知又走完整 drain 重复回灌，全 wf- 时整条不注入）。**(2) /workflows 面板（Q5）**：`main.py:_dispatch_workflows_command`（`/workflows` / `list` / `<run-id>` / `clear`，纯人看、never-raise、读 registry locked 快照），纯函数 `_format_workflow_run_line`/`_format_workflow_run_detail`/`_humanize_age`（注入 `now` 可单测）；登记进 `_SLASH_COMMANDS`+`_HELP_TEXT`。不做 cancel（与后台子代理不可中途取消一致）、不给 LLM 暴露查询工具（结果靠异步回灌）。**(3) resume（Q3=a+a）**：schema 加可选 `resume_from: <run_id>`（LLM 驱动，对标 CC `resumeFromRunId`，run id 在 summary footnote/通知里返回）。纯函数 `core/workflow.py:compute_resume_plan(spec, prior_spec, prior_results)` 算复用集：缓存命中 = 同 `id` 节点存在 **且** 上次 `DONE` **且**原始 `prompt` 文本未变（直接比对，不含 upstream 注入）→ 复用上次 output（不重跑、不计 budget、`NodeResult.reused=True` 标记）；`FAILED`/`SKIPPED`/新增/prompt 改动 → 重跑，且**保守级联**——任一节点重跑则其所有 transitive dependents 即使 prompt 未变也强制重跑（memoized DFS，cache 防环）。`run_workflow` 加 `reused_results` 参数 seed 复用节点为 DONE（scheduler 跳过执行、产物喂下游）。`resume_from` 指向不存在的 run（registry 已 clear/跨会话）→ **fail-open 整图重跑**。**(4) token budget（Q4=a+新管线+a）**：schema 加可选 `token_budget: int`（per-call）+ `[workflow] default_token_budget` config 兜底（0=不限，per-call 优先）。**新增计量管线**：`run_subagent`/`_run_subagent_sync` 加 `token_tracker` 参数透传进节点内层 `agent_loop`；`_install_workflow_handler` 闭包给**每个节点**建独立 `TokenTracker`（线程局部、零竞争），节点跑完把 `total_tokens` 在 `budget_lock` 内累加到 run 级计数器（`tokens_spent` 闭包读）。`run_workflow` 加 `token_budget`+`tokens_spent` 回调：**每层启动前**检查 `tokens_spent() >= budget`，超则剩余 PENDING 节点全标 SKIPPED（`_BUDGET_EXHAUSTED_REASON` 哨兵，`format_summary` 据此加"Stopped early"标注），在跑的层正常跑完（**层粒度软护栏**，非 mid-call 硬切断）。**(5) WorkflowRegistry（`src/core/workflow_registry.py`）**：内存进程级 + 会话作用域 + **带锁**（后台线程写 + 主线程读快照）的 run 存储，`wf-<rand8>` id、FIFO `max_runs`（默认 50，优先裁已完成）、`start`/`update_node`/`set_tokens`/`finish`/`get_for_resume`/`snapshot`/`get`/`take_undelivered`/`clear_finished`/`clear`；缺失 run 的更新静默 no-op（容会话切换后 lingering 后台线程）；快照 copy results dict 保证一致性视图。生命周期镜像 `subagent_registry`/`spawned_agents`——REPL 实例化一次，`/new`·`/resume`·`/import` clear、`/compact` 保留；`run_workflow` 加 `on_node_status` 回调把每节点终态实时写 registry 供面板。`_install_workflow_handler` 先 `validate_workflow_input`（拆出的 parse+validate，给后台路径即时报错）→ `registry.start` → 建 budget 计量 + execute_node → 同步 `drive()`/后台 `submit`。MVP 不做：落盘跨重启 resume（仅内存）、取消运行中 workflow、循环/条件/动态 fan-out、节点级精确 budget 计费、嵌套 workflow、worktree per-node。关键文件：`src/core/workflow.py`（`compute_resume_plan`+`run_workflow` 的 `reused_results`/`token_budget`/`tokens_spent`/`on_node_status` 参数 + `NodeResult.reused` + summary 标注）、`src/core/workflow_registry.py`、`src/core/handlers/workflow.py`（schema 三字段 + `validate_workflow_input` + `run_workflow_tool` 扩展）、`src/main.py`（`_install_workflow_handler` 重写 + `_drive`/`_drain_workflow_results`/`_dispatch_workflows_command`/面板格式化 + `WorkflowRegistry` 宿主 + `pending_workflow_messages` + 会话切换 clear + `WorkflowConfig.default_token_budget`/`max_runs`）、`src/planning/subagent.py`（token_tracker 透传）、`src/concurrency/notification.py`（wf- 跳过）。配置见 `config.toml [workflow]`。

### 多智能体协调 (`src/team/`)
`MessageBus`（基于 JSONL 的追加式邮箱）、`ProtocolFSM`（带轮询的请求-响应协议）、`AutonomousAgent`（守护进程式空闲-轮询-认领循环）、`TeammateManager`。协议：PLAN_APPROVAL、SHUTDOWN。

**子系统完善（task 06-06-team-subsystem-completion）**：把 team 从"LLM 用不上的半成品"补到"可用协作闭环"。**(1) 请求-响应回路闭合到 LLM**：`main.py:_team_send` 升级为**阻塞式**——发出 request 后用 `ProtocolFSM(bus, MAIN).wait_response(msg_id, timeout=team.response_timeout)`（复用现成 FSM）等队友回复，把回复作 tool_result 返回 LLM；目标为 `MAIN_AGENT_NAME` 或队友未在运行（经 `bg_manager.is_running` 预判）则**不阻塞**、立即返回清晰说明（避免干等满超时）；超时返回"N 秒内无回复"提示。`main.py:_drain_team_mailbox` 从"只 `print_status`"升级为"**也回灌 LLM**"——但不直接 append 新 user 消息（会破坏 Anthropic 角色交替），而是把迟到/未经请求/广播消息收进 REPL 会话作用域的 `pending_team_messages` 缓冲，在**下一个 user turn** 前缀注入。**两条通路去重**：阻塞 `team_send` 消费回复后调 `MessageBus.mark_delivered(response.id)`，drain 用 `was_delivered` 跳过——避免同一回复既作 tool_result 又被 drain 重复回灌。去重集 `_delivered`（带 `_delivered_lock`）**挂在 MessageBus 实例上**，因 `_switch_session_mailbox` 每会话新建 bus（`/new`·`/resume`·`/import`），去重集随之自动重置、`/compact` 同会话保留——零额外线程穿透（bus 本就穿到 `_team_send` 与 `_drain_team_mailbox` 两处）；`pending_team_messages` 在三个会话切换点 `.clear()`（镜像 `spawned_agents` 生命周期）。**(2) 异常隔离**：`autonomous.py:_handle_messages` 给请求路径包 try/except，出错经 `_protocol.respond` 回 `[error] <类型>: <消息>` + `logging.exception`，守护线程存活继续循环（否则一条坏请求穿出 `run()` 杀死后台线程、静默停摆、还让阻塞的 `team_send` 干等满超时）。**(3) 单队友停止**：`team_shutdown <name>` 工具（schema 在 `core/tools.py:TEAM_TOOL_SCHEMAS` + 入 `DEFERRED_TOOLS` + fallback + `_make_team_handlers` 返回 + `_handle_team_command` slash 子命令）向目标发 SHUTDOWN 协议消息（接收侧 `autonomous.py` 的 SHUTDOWN 检查在 msg_type 过滤之前，故任意 msg_type 都触发）+ 从 `spawned_agents` 剔除；权限档同 `team_send`（非 SAFE）。**(4) 活性检测**：`concurrency/background.py:BackgroundManager.is_running(task_id)` 只读探活（lock-guarded 复用 `is_alive()`，不剪枝）；`_team_list` 改用 `is_running(f"team:{runtime_id}:{name}")` 反映真实线程存活，取代旧的"看 `spawned_agents` 字典在不在"（从不剔除崩溃/结束的队友）。**(5) 响应速度**：`autonomous.py:run` 两处 `time.sleep(poll_interval)` → `bus.wait_for_message(self.name, timeout=poll_interval)`（邮箱已有的条件变量原语）——有信立刻醒、无信仍周期醒来扫任务，直接提升阻塞问答往返速度（`import time` 随之移除）。**(6) 配置 `[team]`**：`TeamConfig(poll_interval=1.0, response_timeout=60.0)` + `_parse_team_config`（逐字段容错、非正数回退默认）；boot 固化、restart-required（generic `_flatten_config` 自动归 restart）。**Out of Scope**（后续单元）：队友有状态记忆/SendMessage 续跑（与 subagent 续跑统一设计）、PLAN_APPROVAL 发送侧接线（接收侧已存在，不删不接）、`team_register` 动态创建队友（手改 `.team.json`）、邮箱 O(n²) 轮转、显式异步 `team_receive` 双步协议。关键文件：`src/team/{autonomous,mailbox}.py`、`src/concurrency/background.py`、`src/core/tools.py`、`src/main.py`（`TeamConfig`+`_parse_team_config`+`_make_team_handlers`+`_drain_team_mailbox`+`pending_team_messages`+`_handle_team_command`）。配置见 `config.toml [team]`。

**队友有状态记忆（task 06-08-team-stateful-memory）**：把队友从"逐请求无状态、跑完即弃"升级为"跨 request 保留对话上下文"——子代理 SendMessage 续跑（task 06-06-subagent-sendmessage-team）补齐了"前台子代理可续跑"，本任务把同一 SendMessage 母题落到 team 队友（`team_send X 做 A → 追问基于 A 的 B` 时 X 记得 A）。**(1) 记忆范围（Q1，决策 a）**：**仅 request 路径续记忆**——队友连续收到的 `team_send` request 累积进一条 live `self._messages`，agent_loop 原地追加 turn（复用 subagent_send 的 live-messages 范式：loop.py:111/181 原地 append assistant+tool_result），下条 request 续在其后；**自认领的 TaskManager task 永远无状态**（`_execute_task` 仍走现建 `messages=[]` 的 `_run_prompt`，task 是离散单元、不与对话互污染）。`_handle_messages` 按 `memory_enabled` 分派：开则 `_run_request`（有状态）、关则 `_run_prompt`（旧无状态行为，字节级一致）。system_prompt 在 `__init__` 时一次性 seed 进 `self._messages`（有状态路径不再每 turn 重复 prepend）。**(2) Compactor 注入（Q2，决策 a）**：`AutonomousAgent` 加 `compact_fn`（默认模块级 `_noop_compact`）+ `memory_enabled`（默认 False，向后兼容）两参数；`main.py:_team_spawn` 在 `config.team.memory_enabled` 开时给每个队友建 **per-teammate** `Compactor(provider=队友provider, transcript_mgr=None, session_id=f"team:{name}")` 注入——对齐主循环注入 compact_fn 范式、AutonomousAgent 与 Compactor 解耦可单测；阈值不单独暴露（复用 Compactor 默认 50000，与主循环 base_compact_fn 一致）。**(3) 失败回滚（Q3，决策 a）**：`_run_request` 进 turn 前 `snapshot = len(self._messages)`、append user 后跑 loop、`except BaseException: del self._messages[snapshot:]; raise`——一次瞬时失败不毒化记忆线的角色交替不变量，下条 request 从干净 assistant 边界续；`_handle_messages` 现有 try/except 照旧 respond 错误文本。复刻 `/goal` `_drive_goal` 回滚 + subagent_send "成功才注册" 安全范式。**线程安全**：`self._messages` 仅被队友自己的单线程 `run()` 读写，无需锁（与 Scheduler 跨线程碰 console 不同）。**配置 `[team] memory_enabled`**（默认 True + env `BAREAGENT_TEAM_MEMORY_ENABLED` 经 `_resolve_bool`，对齐 retry/cache/workflow 的 enabled 旋钮；`_parse_team_config` 逐字段容错；boot 固化、restart-required）；关掉回退逐请求无状态旧行为。MVP 不做：跨 `/resume` 把队友记忆持久化到磁盘（进程内、随队友 daemon 线程生命周期）、每个 requester（from_agent）独立记忆线、队友 transcript 落盘（Compactor 的 transcript_mgr 传 None）、可调压缩阈值（YAGNI 后置）。关键文件：`src/team/autonomous.py`（`_messages`+`_run_request`+`_noop_compact`+注入参数+回滚）、`src/main.py`（`TeamConfig.memory_enabled`+`_parse_team_config` env+`_team_spawn` per-teammate Compactor 注入）、`src/memory/compact.py`（复用不改）。配置见 `config.toml [team]`。

**team 收口：team_register + PLAN_APPROVAL 发送侧（task 06-08-team-register-and-plan-approval）**：补齐 06-06-team-subsystem-completion 列为 Out of Scope 的两个遗留半成品，两者都复用现成机制、仅补缺失的接线点。**(1) `team_register`**（动态建队友，Q1 决策 a 扁平串）：`team_register(name, role, system_prompt, provider?, model?)` 把 `TeammateManager.register`（已存在，校验非空 + 落 `.team.json` + RLock）接成 client tool；`provider`/`model` 可选，内部组 sparse `provider_config={name,model}`（丢空值），省略则继承会话 provider（`_make_teammate_provider_factory` 同名继承 api_key_env/base_url/wire_api）；罕见 override 不进 MVP（手改 `.team.json`）。never-raise（register 抛 ValueError → `Error: <msg>`）。**不自动 spawn**（register/spawn 两步，对齐 TeammateManager 拆分）。**(2) `team_request_review`**（PLAN_APPROVAL 发送侧，Q2 决策 a 独立工具）：接收侧（队友 `_build_incoming_prompt` 识别 PLAN_APPROVAL 包成审阅 prompt + `respond` 同协议回）06-06 已全通，只差发送侧调用点。`team_request_review(to_agent, plan)` **复刻 `_team_send` 阻塞骨架**——main-target/unknown-teammate/not-running 立即返回清晰说明（避免干等满超时）、`ProtocolFSM(bus, MAIN).request(to, Protocol.PLAN_APPROVAL, plan)` 编码协议发出、`wait_response(timeout=team.response_timeout)` 阻塞拿裁决、`decode_protocol_content` 取 body 回灌 LLM、`mark_delivered` 去重（与 drain 两通路不重复回灌，同 team_send）。**不接管 exit_plan_mode**（独立 LLM 可调工具，plan 模式联动 out of scope）。**五处接线**（两工具均镜像 team_spawn/team_send）：`core/tools.py` 的 `TEAM_TOOL_SCHEMAS`（`_schema=tool_schema`）+ `DEFERRED_TOOLS` + `_TEAM_FALLBACK_HANDLERS`、`main.py:_make_team_handlers` 返回 dict + `_handle_team_command` slash（`/team register <name> <role> <prompt>` 用 `split(" ",3)` 容多词 prompt、`/team review <name> <plan>`）+ usage/help 文案。**权限**：两工具均**非 SAFE**（未入 `SAFE_TOOLS`，DEFAULT 下确认，对齐现有 team 工具）。MVP 不做：注销/编辑队友（team_unregister/update）、register 自动 spawn、exit_plan_mode 联动、多人并行审批聚合。关键文件：`src/core/tools.py`（2 schema + DEFERRED + fallback）、`src/main.py`（`_make_team_handlers` 加 `_team_register`/`_team_request_review` + `_handle_team_command` slash）、复用 `src/team/manager.py`（register）+ `src/team/protocols.py`（PLAN_APPROVAL，不改）。

### 智能体类型系统 (`src/planning/agent_types.py`)
`AgentType` 冻结数据类定义子智能体配置（工具白/黑名单、max_turns、嵌套控制、权限模式覆盖）。内置四种类型：`general-purpose`（全量工具，可嵌套，200 轮）、`explore`（只读，50 轮）、`plan`（只读，50 轮）、`code-review`（只读，50 轮）。`resolve_agent_type()` 解析类型名称并回退到默认值。`filter_tools()` / `filter_handlers()` 按类型过滤工具和处理器。

### 子智能体委派 (`src/planning/subagent.py`)
隔离的消息上下文，递归深度限制（max_depth=3），基于 token 的消息压缩（50k 阈值）。支持 `agent_type` 参数选择智能体类型，`run_in_background` 参数后台异步执行。权限隔离：通过 `PermissionGuard.for_subagent()` 创建子级权限，后台智能体使用 fail-closed 模式。

### 子代理 SendMessage 续跑 (`src/planning/subagent_registry.py` + `src/core/handlers/subagent_send.py`，task 06-06-subagent-sendmessage-team，对标 Claude Code `SendMessage`)
把子代理从"一次性问答、跑完即弃"升级为"可被追加消息续跑、上下文原样保留"——LLM 用 spawn 时返回的 agent id 经 `subagent_send` 继续此前的子代理对话。**纯模块 `subagent_registry.py`**（零 LLM/loop 依赖，仿 retry/goal/workflow 可单测）：`ResumableContext`（agent_id + **live** `messages` 列表 + 重入 `agent_loop` 所需全部绑定：provider/tools/handlers/permission/compact_fn/max_turns/retry_policy；`messages` 由 `agent_loop` 原地追加故续跑后自动最新）+ `SubagentRegistry`（`register`/`get`/`has`/`clear` + `generate_id`→`sa-<rand8>`；**FIFO 软上限**：插入序 dict，register 用 pop+重插实现 move-to-end，超 `max_resumable` 弹最旧——move-to-end 确保多轮对话中的活跃上下文不被误淘汰）。**注册时机**：`run_subagent`/`_run_subagent_sync` 加 `registry` 参数，**仅当 `registry is not None and isolation == "none"` 且 loop 成功完成**（reach 到注册点即 agent_loop 未抛）才注册并在 result 尾附续跑 ID 脚注。**只注册主循环直接 spawn 的前台 none-isolation 子代理**：后台路径的 `bg_manager.submit` partial 传 `registry=None`（fire-and-notify、上下文随线程消亡）、`_run_subagent_sync` 内嵌套 subagent 闭包传 `registry=None`（"只能续跑你自己 spawn 的 agent"）、worktree 隔离因 `isolation != "none"` 天然不注册（其隔离树在 loop 末 finalize，续跑会写错地方）。**`subagent_send` 是主循环专属工具**：schema + 纯逻辑 `run_subagent_send(agent_id, message, *, registry, run_loop)`（注入 `run_loop` 可单测：校验非空 → 查表缺失返回 `Error:` never-raise → 追加 user 消息 → `run_loop(ctx)` 重入 → 成功后 `registry.register(ctx)` move-to-end 刷新位置 → 返回结果 + 续跑脚注）在 `core/handlers/subagent_send.py`；**子代理隔离三重保障**——入 `agent_types.MAIN_LOOP_ONLY_TOOLS`（`filter_tools` 对所有子代理类型恒剥离）+ 不进 `get_tools()` 全局集（schema 只 append 到 `loop_tools`）+ `filter_handlers` 丢孤儿 handler；handler 经 `main.py:_install_subagent_send_handler`（`run_loop` 闭包只需绑 registry，其余绑定全在 ctx）在 boot + 4 个会话切换点重装（仿 `_install_workflow_handler`）。**权限档对齐 `subagent`**：`subagent_send` 不入 `SAFE_TOOLS`（续跑会触发写）——DEFAULT 确认 / PLAN 拒绝 / BYPASS 放行。**会话作用域生命周期**：`subagent_registry = SubagentRegistry(config.subagent.max_resumable)` 在 REPL 实例化一次（紧邻 `spawned_agents`），`.clear()` 于 `/new`·`/clear`（共用块）·`/resume`·`/import`、`/compact` 保留——穿入 `_build_handlers`→`get_handlers` 让主循环 subagent 闭包持有它（teammate 的 `_build_handlers` 调用不传 → 队友 spawn 的子代理不污染主注册表）。配置 `[subagent] max_resumable`（默认 20，config-only 容错解析 <1 回退，restart-required）。MVP 不做：后台/worktree 子代理续跑（生命周期与"持有可重入上下文"本质冲突）、跨 `/resume` 持久化、续跑中途异常的 messages 回滚（成功才注册已大幅收敛风险）、team 队友记忆（独立后续任务，需另解无界增长+压缩+角色交替）。关键文件：`src/planning/subagent_registry.py`、`src/core/handlers/subagent_send.py`、`src/planning/subagent.py`（`registry` 参数 + 注册）、`src/core/tools.py`（`get_handlers` 穿透）、`src/planning/agent_types.py`（`MAIN_LOOP_ONLY_TOOLS`）、`src/main.py`（`SubagentConfig.max_resumable` + registry 宿主 + `loop_tools` + `_install_subagent_send_handler` + 会话切换 clear）。配置见 `config.toml [subagent]`。

### Git Worktree 子代理隔离 (`src/planning/worktree.py`)
`run_subagent(isolation="worktree")`（schema 暴露 `isolation: "none"|"worktree"`，LLM 可请求）让子代理在**独立的 git worktree + 临时分支**中工作，所有文件操作（bash/read/write/edit/glob/grep）落在隔离工作目录，不污染主工作区。对齐 ROADMAP 3.3 / Claude Code `Agent(isolation:"worktree")` 语义。`worktree.py` 是纯 git CLI 封装（无 LLM/loop 依赖，可单测），镜像 `context.py:_run_git_command` 的 subprocess 范式（utf-8 / errors=replace / timeout）：`is_git_repo`、`create_worktree`（worktree 落系统临时目录 `tempfile.mkdtemp(prefix="bareagent-wt-")`，分支 `bareagent/wt-<id>`，失败抛 `WorktreeError`）、`worktree_status`（`git status --porcelain` 非空即 dirty）、`remove_worktree`（`worktree remove --force` + `branch -D`，best-effort 幂等）。隔离核心是 `core/tools.py:rebind_workspace_handlers`——浅拷贝 handlers，只把 6 个文件 handler 的 partial 重绑到 worktree 路径（`write_file`/`edit_file` 从原 partial 的 `.keywords["diagnostics_hook"]` 取回复用），其余 handler 原样保留。生命周期全在 `_run_subagent_sync` 内（后台路径天然继承），重绑在 readonly-memory 包装之后、嵌套 subagent 闭包之前（确保 worktree 内再 spawn 的子代理也用 worktree 文件 handler，嵌套 isolation 默认 "none"，见 Out of Scope）；loop 结束后 `try/finally` 按 dirty 决定保留+回报路径/分支或自动清理，脚注追加到 result 尾部。**fail-open**：非 git 仓库 / worktree 创建失败 → 回退无隔离继续跑 + 脚注提示（隔离是便利层，安全边界仍是 PermissionGuard，与 hooks 一致）。worktree 的 git 命令不经 PermissionGuard（基础设施级，同 task.py/context.py）；子代理在 worktree 内的 bash/write 仍受 `child_permission` 约束。MVP 不加配置项（temp 前缀 / 分支名硬编码），不支持嵌套 worktree、自动 commit/merge/PR、worktree 内 LSP 重新 rooted（diagnostics 仍指向主 repo root）。关键文件：`src/planning/worktree.py`、`src/core/tools.py:rebind_workspace_handlers`、`src/planning/subagent.py`（`isolation` 参数贯穿 + `_finalize_worktree`）。

### 技能系统 (`src/planning/skills.py`)
从 `skills/*/SKILL.md` 自动发现技能。通过 `load_skill` 工具按需加载。当前技能：code-review、git、test。`SkillLoader` 支持**多扫描根**：仓库 `skills/`（checked-in 正典层）+ 可选 `generated_root`（经验式习得层，见下），两层都进开局列表 + 都可 `load_skill`，同名**正典优先**（先扫仓库，generated 重名跳过）。

### 经验式技能生成 (`src/planning/skill_gen.py` + `skill_store.py`，对标 Hermes Agent)
复杂的多轮任务收尾后，agent 自动把"这次怎么做成的"沉淀成可复用 SKILL.md 草稿，下次同类任务自动获得这份经验。对标 Nous Research **Hermes Agent** 的 `skill_manage` 自动生成 skill，但裁剪掉其全自主静默写 + 自进化的高风险部分，落到适合单人个人工具的**半自动**档：**自动起草 → 草稿区 → 用户提升**。**触发（决策3）**：`SkillGenerator`（纯逻辑、可单测，仿 `retry.py` 范式）维护跨 turn 累计计数器，`agent_loop` 每个 turn 自然收尾（stop、未失败/中断）时调 `skill_gen.note_turn(turn_tool_calls)`；**双条件 AND**（`should_draft_skill`：累计 `工具调用 ≥ min_tool_calls`(默认5) 且 `用户回复 ≥ min_user_replies`(默认3)）命中后由 REPL 触发**一次隔离的反思 LLM 调用**起草，随后 `reset()` 重置计数（一段多轮工作流打包成一个 skill）。**反思调用**（`main.py:_run_skill_reflection`）在**消息副本**上跑独立 `agent_loop`（`tools=[SKILL_CREATE_TOOL_SCHEMA]`、`skill_gen=None`、低 `max_iterations`），故真实会话历史/turn 返回值不被污染；模型可回 "no skill" 拒绝（低价值任务的第二道质量闸）。**`skill_create`（决策4，create-only）刻意不进全局工具集**，只在反思调用里暴露——天然实现"主循环触发、子代理拿不到、`auto_generate=false` 全链路短路"三件事（工具压根不存在）；入 `PermissionGuard.SAFE_TOOLS`（草稿写 `.pending/` 沙箱不弹确认）。**存储（决策2，`skill_store.py:SkillStore`）**：生成 skill 根 = `~/.bareagent/projects/<slug>/skills/`（复用 `derive_memory_slug`，项目隔离、不进版本控制，可配 `[skills] dir`），与仓库正典分离；草稿落 `.pending/<slug>/SKILL.md`，`/skill keep` 提升（挪到 live 根）、`/skill discard` 删除、pending 数量软上限 `max_pending`（默认10，按最旧裁剪，非时间 TTL）。生成 SKILL.md 沿用 BareAgent 轻格式（目录名=skill 名、首行=描述，匹配 `_extract_description`），**不引入** Hermes frontmatter（version/tags/category）。REPL 命令 `/skill`（list | keep <name> | discard <name>，never-raise）。`note_turn` 在 `/clear`·`/new` 重置。不做定时自评、不做自动提升、不做糊复杂度信号（撞错恢复/用户纠正）。配置见 `config.toml [skills]`（`auto_generate` 走 env `BAREAGENT_SKILLS_AUTO_GENERATE`，余 config-only，`_parse_skills_config` 逐字段容错；`_build_skillgen_config` 做 `SkillsConfig`→`SkillGenConfig` 转换），boot 固化随 provider 走 restart-required（不进 hot 集）。关键文件：`src/planning/skill_gen.py`、`src/planning/skill_store.py`、`src/planning/skills.py`（多根）、`src/core/handlers/skill.py`、`src/core/loop.py`（`skill_gen` 参数 + 计数）、`src/permission/guard.py`（SAFE_TOOLS）、`src/main.py`（`SkillsConfig` + 解析 + 构造 + 反思 + `/skill`）。**自进化（task 06-01-skill-self-evolution）**：反思起草时若识别到这次工作流是某个**已有生成 skill** 的改进，就**用同名取代**它而非堆近似重复。复用既有 create→promote 同名替换机制，真正新增的只有"让反思模型看见已有 skill 内容"：candidates = `SkillLoader(store.root).scan()`（仅生成 live，pending 被一层 glob 排除、正典因单扫生成根而排除）的 (名,描述) 注入反思 user 消息（`skill_gen.render_reflection_prompt`，空候选退回纯 `DRAFT_INSTRUCTION` 字节级兼容）；**有候选时**反思工具集加只读 `load_skill`（`*LOAD_SKILL_TOOL_SCHEMAS`，读写边界仍干净：读旧全文→同名 `skill_create` 写改进版），无候选则退回纯 create-only（`max_iterations` 6 容纳 load+create+收尾）。**正典保护**：`run_skill_create` 加 `reserved_names`（= `SkillLoader.canon_skill_names()` 只扫 `skills_dir`），slug 撞正典名即返回 Error 不写盘（否则生成同名会被正典 shadow 成死 skill；对新建+进化都生效）。进化产物仍走 **pending → 用户 `/skill keep` 取代 live**（不直接改 live、不留备份），`/skill list` 给 pending 中与 live 同名者标注 `(revision of live '<name>')`。MVP 不做：patch/edit 增量更新（`skill_update`，全量重写够用）、向量/词法相似度匹配（LLM 判断足够）、版本历史/回滚/`/skill restore`、自动提升、改写仓库正典（均为后续单元）。

### 任务与 TODO 管理 (`src/planning/`)
`TaskManager`（tasks.py）：持久化 JSON 存储，状态（pending/in_progress/done/failed），依赖追踪。`TodoManager`（todo.py）：内存级会话作用域，优先级，提醒机制。

### 消息压缩 (`src/memory/compact.py`)
微压缩截断旧工具结果；完整压缩通过 LLM 生成摘要。基于阈值触发（50k tokens）。保留系统消息和近期上下文。

### 持久化记忆 (`src/memory/persistent.py`)
文件式跨会话记忆：一条记忆 = 一个带 frontmatter（name/description/metadata.type，type ∈ user/feedback/project/reference）的 `.md` 文件，`MEMORY.md` 作单行索引。`MemoryManager` 暴露六个文本编辑器式命令（`view`/`create`/`str_replace`/`insert`/`delete`/`rename`），契约对齐 Anthropic memory tool，但注册为**单个普通 client tool** `memory`（schema 在 `core/tools.py:MEMORY_TOOL_SCHEMAS`，handler `core/handlers/memory.py:run_memory` 薄封装委派给 manager），不绑定原生 `memory_20250818` 类型，故全 provider 通用。所有路径经 `core/sandbox.py:safe_path` 限制在记忆根内（兼容 strip `/memories/` 前缀），写入走 `fileutil.atomic_write_text`。存储位置可配置 `[memory] dir`，默认全局 `~/.bareagent/projects/<workspace-slug>/memory/`（slug 由 `derive_memory_slug` 派生）。会话开局 `MemoryManager.system_prompt_section()` 把 MEMORY.md 索引（前 `max_index_lines` 行）+ MEMORY PROTOCOL 注入 `assemble_system_prompt`。**召回层（recall layer，仿 Claude Code 相关性召回）**：`MemoryManager.recall(query, k)` / `recall_section(query, k)` 按 frontmatter `name + description`（缺失回退正文前 200 字）做跨语言词法匹配（ASCII 词 + 中文滑动 bigram），取 top-K 拼成 `<memory-recall>` 块；`main.py:_refresh_memory_recall`（仿 `_refresh_nag_reminder`，在 `_build_loop_compact` 的 `_compact` 里每轮 agent_loop 调用）剔除旧块并在最后一条真实 user 消息后注入新块，故 `/remember`、`/forget`、普通 user-turn 均自动获得逐轮召回注入，与开局索引注入互补。`recall_k`（`[memory] recall_k`，默认 5，0 = 关闭召回仅保留索引注入）控制条数。**语义/向量召回（task 06-08-semantic-memory-recall）**：`recall()` 升级为可注入 embedder 的可插拔召回——`MemoryManager(embedder=...)` 注入后 `recall` 走 `_semantic_recall`（按 embedding 余弦相似度 top-K，换了措辞也能命中），embedder 为 None 或调用抛错则 fail-open 回退 `_lexical_recall`（词法行为字节级不变）；工具表面（`recall`/`recall_section`/`RecalledMemory`，`score` 由 int 放宽为 float）不变（兑现原 docstring "vector backend 替换 method body 不动工具表面"承诺）。`src/memory/embedding.py`（纯逻辑可单测）：`Embedder` Protocol + `OpenAIEmbedder`（复用 openai client 打 `/embeddings`）/ `LocalEmbedder`（lazy import fastembed，`[embeddings]` extra）两 backend + `build_embedder`（fail-open 工厂，缺 dep/缺 key/未知 backend → None）+ 纯 Python `cosine`（不给 openai backend 强引 numpy）+ `EmbeddingCache`（记忆根下 `.embedding-cache.json`，`{relpath:(hash,vector)}` + `identity=backend:model` 整体失效、content-hash 单条失效、坏文件 start-fresh）。`_semantic_recall` 懒算：只 embed 缓存缺失/hash 变更的记忆（批量）+ query，prune 已删文件，回写缓存。配置 `[memory]`：`semantic_recall`（默认 **false** opt-in + env `BAREAGENT_MEMORY_SEMANTIC_RECALL`；保持词法行为兼容）、`embedding_backend`（`openai`|`local`）、`embedding_model`（空=backend 默认 `text-embedding-3-small`/`BAAI/bge-small-en-v1.5`）、openai 的 `embedding_base_url`/`embedding_api_key`（空则 `main.py:_build_memory_embedder` 复用会话 provider 的，经 `factory._resolve_api_key`）；`build_embedder` 返回 None 时打一条 status 提示并继续词法。`system_prompt_section()` 的开局索引仍是 MEMORY.md 注入（本任务只动 recall 层）。MVP 不做：向量 DB（工作集小，暴力余弦足够）、词法+语义混合打分、rerank、相似度阈值过滤（纯 top-K，`score<=0` 排除负相关）。关键文件：`src/memory/embedding.py`、`src/memory/persistent.py`（recall 分派 + `_semantic_recall`/`_lexical_recall`）、`src/main.py`（`MemoryConfig` 字段 + `_build_memory_embedder` 穿透）、`pyproject.toml`（`[embeddings]=["fastembed>=0.8"]`）。权限：`memory` 入 `PermissionGuard.SAFE_TOOLS`（不弹确认，沙箱内 bookkeeping）。子代理只读隔离：`AgentType.memory_writable`（explore/plan/code-review 默认 False）——单工具无法按子命令名过滤，故由 `subagent.py:_make_readonly_memory_handler` 在子代理边界包装 handler、拒绝五个写命令、放行 `view`。REPL 命令：`/remember <文本>`、`/forget <文本>`（注入用户指令驱动 LLM 经工具落盘/删除并维护索引）。配置见 `config.toml [memory]`。

### 语义代码检索 (`src/memory/code_index.py` + `src/core/handlers/code_search.py`，task 06-19-repo-map-lsp-documentsymbol-embedding)
让 agent 不靠盲读整文件 / 反复 grep 就能按语义取相关代码块——`code_search(query, k=8, path=".")` 工具经 embedding 余弦相似度返回 top-K 代码块（`file:start-end` + 片段）。对标 Cursor 语义检索（语义+grep 结合实测准确率 +12.5%）。**复用 `memory/embedding.py` 整套**（`Embedder` Protocol / `OpenAIEmbedder`·`LocalEmbedder` / 纯 Python `cosine` / `text_hash` / `EmbeddingCache` 失效范式），新增的只是 *code* 特有部分。`code_index.py` 是**纯模块**（零 LLM/loop/REPL 依赖，注入式 `Embedder` 可单测，仿 embedding/persistent 范式）：`CodeChunk`/`CodeSearchResult`（frozen+slots）+ `chunk_lines`（**固定行窗分块**：`chunk_lines` 行/块 + `chunk_overlap` 行重叠，1-based inclusive，语言无关零解析器依赖；防御 overlap>=window 时 step 钳为正不死循环、丢尾部空行、全空白段跳过）+ `CodeIndex`（**懒增量索引**，仿 `_semantic_recall`：首次 `search` 才构建，`_collect_chunks` 走 `iter_search_files`（跳过 .git/__pycache__/node_modules/.venv）+ 1MB 上限 + utf-8 解码失败跳过，cache key=`relpath#startline` 只 embed 新增/变更 chunk、`prune(live_keys)` 清删除文件/缩短尾部、identity 整体失效；cosine 排序 `score<=0` 排除 + `(-score, relpath, start_line)` 确定性 tie-break）。**fail-open 彻底**：embedder=None / `embed` 抛错 / 坏缓存 → 返回 `[]`，handler 回友好提示引导 `grep`，**绝不崩**（embedding 是增强非硬依赖）。**boot 门控**：schema 在 `core/tools.py:CODE_SEARCH_TOOL_SCHEMAS`（**不进** `DEFERRED_TOOL_SCHEMAS`），仅当 `_build_code_index` 返回非 None 时 `get_tools()` 才 append、`get_handlers()` 才接 handler——**无可用 embedder 则工具压根不暴露**（不给 LLM 死工具，仿 MCP/LSP「配好才出现」）。`main.py:_build_embedder_from_memory_config` 把 embedder 构造**从 `semantic_recall` 开关解耦**（即使不开语义记忆召回也能用代码检索，复用 `[memory]` 的 backend/model/base_url/key 配置）；`_build_memory_embedder` 仍 re-apply `semantic_recall` 闸故记忆召回行为字节级不变。缓存独立落 `~/.bareagent/projects/<slug>/code-index.json`（与 memory recall 的 `.embedding-cache.json` 分离、key scheme 不同、无冲突）。**权限**：`code_search` 入 `PermissionGuard.SAFE_TOOLS`（只读检索不弹确认）；子代理只读可见——**不**进 `MAIN_LOOP_ONLY_TOOLS` 也不进 `_READ_ONLY_DEFAULTS["disallowed_tools"]`，故 explore/plan/code-review 与 `grep` 一样拿得到。handler `core/handlers/code_search.py` 薄封装：校验非空 query → `safe_path` 校验 path 越界 → `index.search` → 格式化 `file:start-end` + 片段（path 子树前缀后过滤），never-raise（`Error:`/友好提示）。配置 `[code_search]`：`enabled`（默认 **true** + env `BAREAGENT_CODE_SEARCH_ENABLED`，关闭则不建 index 不暴露工具）、`k`（默认 8）、`chunk_lines`（默认 50）、`chunk_overlap`（默认 10）、`max_file_bytes`（默认 1MB），`_parse_code_search_config` 逐字段容错不崩 boot，boot 固化 restart-required。MVP 不做：**Repo Map**（LSP/tree-sitter 符号骨架 + 图排名，拆单独后续任务）、符号感知分块（需 tree-sitter/LSP/AST，固定行窗起步，future 可由 repo map 升级）、向量 DB（工作集小暴力余弦足够）、文件 watcher 实时增量（懒构建 + content-hash 增量已够）、rerank/语义+词法混合打分、boot 后台预建、独立 `[code_search]` embedder 配置（复用 `[memory]`）。关键文件：`src/memory/code_index.py`、`src/core/handlers/code_search.py`、`src/core/tools.py`（`CODE_SEARCH_TOOL_SCHEMAS` + boot 门控注入）、`src/permission/guard.py`（SAFE_TOOLS）、`src/main.py`（`CodeSearchConfig` + `_parse_code_search_config` + `_build_embedder_from_memory_config` + `_build_code_index` + 5 处会话切换接线）。配置见 `config.toml [code_search]`。

### Repo Map 结构全景 (`src/memory/repo_map.py` + `repo_map_extract.py` + `src/core/handlers/repo_map.py`，task 06-20-repo-map)
让 agent 不靠盲读整文件就能拿到代码库的**结构全景**——`repo_map(path=".", focus=[], max_tokens=1024)` 工具用 tree-sitter 抽取整库类/函数/方法的**真实签名行**（剥实现体）、按 **PageRank** 排序、贴 token 预算返回。是 code_search 的姊妹件（task 06-19 拆出缓做）：`code_search` 找「相关代码块」、`repo_map` 给「结构」。对标 Aider repo map（tree-sitter + PageRank + 预算二分）。**分层（保可单测）**：(1) **纯核心 `repo_map.py`**（零 tree-sitter 依赖、合成 tag 数据可单测）：`Definition`/`Reference`/`FileTags`/`TagExtractor` Protocol + `build_reference_graph`（文件节点、referencer→definer 边、按提及次数 × 标识符稀有度 `1/definer数` 加权）+ `pagerank`（**手写幂迭代**，无 networkx；personalization 向量 + dangling 重分布 + 收敛）+ `resolve_focus`（focus 项匹配文件名→该文件 / 匹配定义标识符→其定义文件，喂 personalization）+ `render_file`（按文件分组、**靠 tree-sitter 节点行范围包含关系算嵌套缩进**、每符号带行号、多行签名折叠成单行）+ `format_repo_map`（**token 预算二分**：按 rank 序取最大文件前缀使渲染 ≤ max_tokens，top 文件超预算则行级截断不空手；近似 token=chars/4，无 tiktoken）+ `RepoMapCache`（复刻 `EmbeddingCache` 范式：per-file `(content-hash, FileTags)`、identity 整体失效、`prune` 返回删除数驱动保存、原子写）+ `RepoMapIndex`（懒增量编排：walk → 缓存命中跳过/未命中调注入 extractor → 建图 → PageRank → **focus-first 排序** → 渲染；fail-open 返回 ""）。(2) **抽取层 `repo_map_extract.py`**（需 tree-sitter、guarded import、fail-open）：`TreeSitterExtractor` 按扩展名路由、懒建 per-language `(Language, Parser, Query)`、跑查询收 `@definition.*`/`@reference.*`/`@name` 捕获、**签名靠 node 的 `body` 字段切取**（`source[node.start:body.start]`，语言无关；无 body 字段则首行兜底）；`build_extractor()` 返回 None（extra 未装/无 grammar）→ 工具不暴露。(3) **会话层 `core/file_recency.py`**：`FileRecencyTracker`（纯类、有界 MRU、move-to-end），主循环 read/edit/write 经 `tools.py:_with_recency` 包装记录工作区相对路径，repo_map handler 读 top-N 当**自动 focus**（headline 特性）。**关键实现取舍（偏离原 PRD 处）**：① **不用 `tree-sitter-language-pack`**——TSL pack 1.9 是 Rust-native binding，其 `get_language` 返回的 `builtins.Language` 与独立 `tree_sitter.Query` 类型不兼容（`Query(lang, scm)` 拒收），故改用**各语言 grammar 包**（`tree-sitter-python` 等，返回标准 `tree_sitter.Language`）+ **自写最小 `.scm` 查询**（`memory/repo_map_queries/*.scm`，drop-in：加文件+扩展名映射=加语言；自写故无需 vendor Aider、免 Apache 署名）；② TSL pack 的高层 `process()` 虽给 structure 但 **Python 签名为 None 且无 call references**（PageRank 边必需）故弃用；③ **focus-first 硬排序**（focus 文件按 PageRank 排在前、其余按 PageRank 在后）而非纯 personalization——damping 下 teleport 质量(0.15)太弱，弱连接的「最近文件」会被高引用 hub 埋掉，硬排序保证 headline 的「foreground 最近文件」可靠生效（personalization 仍保留，额外提权 focus 邻居）；④ 子代理**继承**主循环（已被 recency 包装的）handlers，故其非隔离 read/edit/write 也会记录 recency（属会话活动信号，可接受；worktree 隔离子代理用 rebind 的未包装 handlers 不记录）。**MVP 语言** = python/javascript/rust/go/java（全自写查询，验证通过）；TypeScript Out of Scope（语法节点异于 JS、无现成查询，后续 drop-in 补）。**boot 门控**（仿 code_search）：schema 在 `tools.py:REPO_MAP_TOOL_SCHEMAS`（不进 `DEFERRED_TOOL_SCHEMAS`），仅当 `main.py:_build_repo_map_index` 返回非 None（`build_extractor()` 成功）时 `get_tools()`/`get_handlers()` 才注入。**权限**：入 `PermissionGuard.SAFE_TOOLS`（只读）；子代理只读可见（**不**进 `MAIN_LOOP_ONLY_TOOLS`/`_READ_ONLY_DEFAULTS["disallowed_tools"]`，同 grep/code_search，靠 handler 继承+agent_type 过滤）。缓存独立落 `~/.bareagent/projects/<slug>/repo-map-cache.json`。**配置 `[repo_map]`**：`enabled`（默认 true + env `BAREAGENT_REPO_MAP_ENABLED`）、`max_tokens`（默认 1024，per-call 覆盖）、`max_file_bytes`（默认 1MB）、`recent_files`（auto-focus 条数默认 5），`_parse_repo_map_config` 逐字段容错，boot 固化 restart-required。**依赖**：optional extra `[repo-map]`（`tree-sitter>=0.25` + 5 个 grammar 包），未装 fail-open 友好提示。MVP 不做：TypeScript、per-symbol PageRank（用文件级）、自动抽取 LLM 提及标识符偏置（走显式 focus）、TSL pack `get_tags_query` 兜底、networkx/tiktoken、文件 watcher、repo_map 自动注入 system prompt（坚持按需工具）。关键文件：`src/memory/repo_map.py`、`src/memory/repo_map_extract.py`、`src/memory/repo_map_queries/*.scm`、`src/core/file_recency.py`、`src/core/handlers/repo_map.py`、`src/core/tools.py`（`REPO_MAP_TOOL_SCHEMAS` + boot 门控注入 + `_with_recency`）、`src/permission/guard.py`（SAFE_TOOLS）、`src/main.py`（`RepoMapConfig` + `_parse_repo_map_config` + `_build_repo_map_index` + `FileRecencyTracker` 宿主 + 会话切换 clear + 接线）。配置见 `config.toml [repo_map]`。

### 用户界面 (`src/ui/`)
`AgentConsole`（基于 rich 的输出）、`StreamPrinter`（流式输出）、`prompt.py`（基于 prompt-toolkit 的输入层）、`theme.py`（主题，默认 `catppuccin-mocha`）。后台通知通过 `concurrency/notification.py` 实现。

### 后台执行 (`src/concurrency/`)
`BackgroundManager`（background.py）：基于 threading 的后台任务管理，支持 submit/drain_notifications。`NotificationManager`（notification.py）：后台任务完成通知。`Scheduler`（scheduler.py，ROADMAP 4.1）：cron 风格定时任务调度——按固定间隔（秒）重复执行 shell 命令。`Scheduler` 只负责「定时 + 重复 arm」：每次 `_fire` 用 `threading.Timer`（daemon）触发后，把命令交给注入的 `BackgroundManager.submit`（用唯一 `loop-<id>-<run_count>` run_id 避开 submit 去重 `ValueError`）在后台线程执行，结果/失败经既有通知通道在下个 turn 浮现；fire 末尾重新 arm 下一个 Timer 实现自重排。`Scheduler` 自身**绝不碰 messages/console**（线程安全关键），`threading.Lock` 保护 job/timer 字典，`_fire` 整体包 try（Timer 线程异常不得逃逸）。`MIN_INTERVAL_SEC=5.0` 守护（防 `/loop 0` 打爆后台），低于报 `SchedulerError`。内存级（退出即清，无跨会话持久化）。REPL 命令 `/loop`：`/loop <秒> <命令...>` 创建、`/loop list` 列出、`/loop cancel <id>` 取消单个、`/loop clear` 清空、`/loop`（无参）= 列表 + 用法。**安全语义**：定时命令经 `run_bash` 但**不经 PermissionGuard 交互确认**（后台无人值守无法弹窗），与 `background_run` 同档（基础设施级）——`/loop` 创建路径与 `_HELP_TEXT` 均明示「runs WITHOUT permission prompts，请自行确保命令安全」。Scheduler 在 `_run_stdio_session` 于 `bg_manager` 之后实例化（runner=`partial(run_bash, cwd=workspace, raise_on_error=True)`），`finally` 里 `cancel_all()`（幂等）清理。MVP 不暴露给 LLM、不支持 cron 表达式 / one-shot 延时 / 次数上限，均为后续扩展位。

### 会话管理
`TranscriptManager`（memory/transcript.py）：会话转录持久化。REPL 支持 `/sessions` 列出历史会话、`/resume` 恢复会话、`/new` 开始新会话、`/clear` 清屏并重置。每个会话有唯一 ID（时间戳格式）。**对话导入导出（`src/memory/conversation_io.py`，ROADMAP 会话便携）**：纯模块（无 REPL/UI/loop 依赖，可单测）暴露 `render_markdown`（人读 markdown：跳过 system、user/assistant 文本、tool_use→单行 `- **Tool call** ``name``: ``input 预览``` 摘要、tool_result→截断代码块、thinking 默认不输出；复刻 `_replay_stdio_transcript` 遍历结构含 `tool_name_by_id` 关联）+ `to_export_json`（自包含 wrapper `{version,session_id,exported_at,messages}`，messages 原样保真含 system/thinking/工具）+ `parse_import`（自动判形：整篇 `json.loads` 成功后 dict 含 `messages`→取之 / 裸 list→直接用，失败回退 jsonl 逐行解析；校验 list[dict] 且每条有 `role`，否则 `raise ValueError(可读原因)`）。REPL 命令：`/export [markdown|md|json] [path]`（markdown 默认，落 `.transcripts/exports/<session>_<ts>.{md,json}` 或显式路径；`_dispatch_export_command` 用 `atomic_write_text` 落盘、整体 try/except never-raise、不经 PermissionGuard 同 /loop 档）；`/import <path>`（读 `.json`/`.jsonl` → `parse_import` 校验 → **载入新会话**：inline **镜像 /resume** 机制——`messages[:]=imported`、`token_tracker.reset()`、新 session id、`_set_compact_session_id`/`_set_interaction_logger_session`、`_switch_session_mailbox`、`spawned_agents={}`、`_build_handlers(runtime_id=new_sid)`、`_replay_stdio_transcript`、`_save_transcript_snapshot`；坏文件/坏 JSON/无 role 在 mutate 前全 `continue`，**fail-safe 零状态改动不崩**）。MVP 不支持「追加到当前对话」、不导出剪贴板/HTML、不批处理多会话（均后续扩展位）。关键文件：`src/memory/conversation_io.py`、`src/main.py`（`_dispatch_export_command` + `/import` inline 镜像 /resume + 命令登记）、`tests/test_conversation_io.py`。

### Token 用量与成本 (`src/memory/token_tracker.py`)
`TokenTracker`：进程级累计 LLM token 用量（`total_input`/`total_output`/`call_count` + 按 model 细分），`record(response, model)` 在 `agent_loop`（`loop.py`）每次 LLM 响应后调用（流式+非流式单点覆盖，可选 `token_tracker` 参数）。REPL `/cost` 命令展示当前会话累计：**总是**显 token 计数 + per-model 细分，有定价的 model 额外显 $ 估算，无价的标 `(no price)`。定价为**混合策略**：内置 Claude Opus/Sonnet/Haiku 4.x 参考价（`DEFAULT_PRICES`，前缀匹配，价格可能漂移），`[cost.prices."<model-id>"]`（单位每百万 token）可覆盖内置价或为任意 model 新增价；未知且未配价的 model 只显 token 不显 $。重置语义：`/new`·`/clear`·`/resume` 归零，`/compact` 不重置（同会话压缩）。配置见 `config.toml [cost]`。

### 追踪 (`src/tracing/`)
统一 Tracer 接口（`_api.py`）+ 代理（`_proxy.py`）+ 配置入口（`setup.py`）。后端：`JsonFileTracer`（始终启用，写入 `.logs/` 供 `/log` 与 web viewer 使用）、`LangfuseTracer`（设 `LANGFUSE_PUBLIC_KEY` 或 `[tracing] langfuse=true` 启用）、`OpenTelemetryTracer`（设 `OTEL_EXPORTER_OTLP_ENDPOINT` 或 `[tracing] opentelemetry=true` 启用）。多后端时通过 `CompositeTracer` 扇出。Langfuse/OTel 为可选依赖，需安装额外 extras。

### 调试与日志 (`src/debug/`)
`InteractionLogger`（interaction_log.py）：将完整 LLM 请求/响应 payload 按会话写入 `.logs/<session-id>/` 的 JSONL，支持订阅事件流。`DebugViewerHandler`（web_viewer.py + viewer.html）：内置只读 HTTP SPA，REPL 中通过 `/log` 命令启动（端口由 `[debug] viewer_port` 控制，默认 8321）。需在配置中将 `[debug] enabled` 设为 `true` 才会写日志。

### MCP 客户端 (`src/mcp/`)
将外部 [Model Context Protocol](https://modelcontextprotocol.io) server 作为可插拔工具源接入 BareAgent。`MCPManager`（manager.py）并发拉起所有 `[[mcp.servers]]`，每个 server 一个 `MCPClient`（client.py）+ `Transport`（transport/，ABC + stdio / http_legacy / http_streamable 三实现）。`registry.py` 把远端工具按 `mcp__<server>__<tool>` 命名注入 `get_tools()` / `get_handlers()`；声明 `resources` capability 的 server 额外得到 `mcp__<server>__resource_list` + `mcp__<server>__resource_read`；`prompts/list` 通过 REPL slash 命令 `/mcp:<server>:<prompt>` 触发。REPL 配套命令：`/mcp status|list|reload`。生命周期硬化：transport reader 线程主动感知 EOF / 断流 → manager 立刻标 UNHEALTHY 并通过 `BackgroundManager.notify` 推送通知；`atexit + SIGTERM` 兜底清理子进程；单次 tool result 在 registry 层按 `max_result_text_bytes` / `max_result_binary_bytes` 截断（256 KiB / 5 MiB 默认）以保护 LLM 上下文。子代理隔离：`AgentType.mcp_tools_enabled`（explore/plan/code-review 默认 False）。关键文件：`src/mcp/__init__.py`、`src/mcp/manager.py`、`src/mcp/registry.py`、`src/mcp/client.py`、`src/mcp/transport/`、`src/mcp/config.py`、`src/mcp/errors.py`。配置见 `config.toml [mcp]` + `[[mcp.servers]]`。

### LSP 客户端 (`src/lsp/`)
通过 [multilspy](https://github.com/microsoft/multilspy)（可选 extra：`uv pip install -e ".[lsp]"`）接入成熟 Language Server，让 LLM 拿到精确的符号导航 + 类型诊断。**multilspy 0.0.15 的语言到 server 映射**：Python → `jedi-language-server`（非 pyright；jedi 适合符号/导航，类型诊断弱）；TypeScript → `typescript-language-server`；Rust → `rust-analyzer`。`LanguageServerManager`（manager.py）按 `[[lsp.servers]]` 并发拉起所有 server，按文件扩展名路由；handshake 失败 / 超时标 UNHEALTHY 不阻塞 REPL boot。`tools.py` 注入四个只读 Tier-1 查询工具到 `DEFERRED_TOOLS`：`lsp_outline` / `lsp_definition` / `lsp_references` / `lsp_diagnostics`（坐标对 LLM 暴露 1-based，内部转 0-based 调 LSP）。**写工具 `semantic_rename(file, line, col, new_name)`**（引用感知的语义重命名，基于 `textDocument/rename`）**故意不带 `lsp_` 前缀**——`lsp_*`=只读查询、`semantic_rename`=写盘，读写边界干净。multilspy 0.0.15 的 `SyncLanguageServer` 无 rename 同步包装，`LanguageServerManager.request_rename` 走裸请求：`asyncio.run_coroutine_threadsafe(server.language_server.server.send.rename(params), sync_server.loop)` + `open_file` didOpen。`src/lsp/workspace_edit.py`（纯函数）解析 WorkspaceEdit 的 `changes` / `documentChanges` 两种形态、按 uri 分组、单文件内按位置倒序应用 TextEdit、`atomic_write_text` 落盘；资源型操作（CreateFile/RenameFile/DeleteFile）MVP 安全跳过并提示（不做文件级重命名）。语义：LSP 不可用 / 无路由 / 空编辑 → 显式 Error 不静默退化为文本替换（无 grep 回退、无 dry-run、无 prepareRename 预校验）。权限：`semantic_rename` 不入 `SAFE_TOOLS`，与 `write_file` 同档——DEFAULT 确认 / AUTO 通过 / PLAN 拒绝 / BYPASS 放行。子代理隔离：因不带 `lsp_` 前缀，加入 `agent_types._READ_ONLY_DEFAULTS["disallowed_tools"]`，explore/plan/code-review 拿不到该写工具。`diagnostics.py` 提供 Hybrid auto-diagnostics-on-edit 钩子（默认 OFF；`[lsp] auto_diagnostics_on_edit = true` 开启后，`edit_file` / `write_file` 成功后通过 diff 算法五元组 `(file, line, col, severity, message)` 计算新增诊断并追加 `Newly introduced diagnostics in <file>:` 段到 tool result）。生命周期硬化：multilspy 0.0.15 默认 `do_nothing` 吃掉所有 `publishDiagnostics`，manager handshake 后直接覆盖 `language_server.server.on_notification_handlers["textDocument/publishDiagnostics"]`，缓存到 `_ServerEntry.diagnostics`；watchdog 线程轮询 subprocess `returncode` 检测崩溃 → 标 UNHEALTHY + console 推送 + `BackgroundManager.notify(f"lsp:{language}", ...)`；`atexit` 注册 `close_all`（幂等，与 MCP 的 atexit 解耦共存）。REPL 命令：`/lsp status|list|reload <language>`。子代理隔离：`AgentType.lsp_tools_enabled`（4 个查询工具只读，explore/plan/code-review 默认 True）；写工具 `semantic_rename` 另由 `disallowed_tools` 黑名单隔离。关键文件：`src/lsp/{__init__,manager,tools,workspace_edit,config,diagnostics,coord,errors}.py`。配置见 `config.toml [lsp]` + `[[lsp.servers]]`（`semantic_rename` 无新增配置项，沿用现有 LSP 配置）。

### Hooks 系统 (`src/hooks/`)
工具调用前后的用户自定义 shell 钩子（ROADMAP 2.1）。用户在 `config.toml` 声明 `[[hooks]]`，BareAgent 在主循环工具执行的前后触发自定义命令。`events.py`：`HookEvent(StrEnum)` 仅两个事件 `PreToolUse` / `PostToolUse`（值对齐 Claude Code）。`config.py`：`HookEntry`（event / command / tool / timeout）+ `HooksConfig`（`matching(event, tool_name)` 按 event 精确 + tool 精确或 None 过滤，保序）+ `parse_hooks_config`（结构错误抛 `HookConfigError` 让 main.py 降级；单条非法 entry 跳过并记入 `skipped` 不整体崩）。`engine.py`：`HookEngine` 匹配 → 构造 JSON → 复用 bash.py 的跨平台 argv（Windows PowerShell + UTF-8 / 非 Windows `bash -lc`）→ `subprocess.run(..., input=json_str)`。**控制协议（exit code，对齐 Claude Code）**：PreToolUse exit 2 = 拦截（跳过 handler，stderr 作拒绝理由回灌 LLM 作 error result）/ exit 0 = 放行 / 其他非 0 = 非阻塞警告 + 放行；PostToolUse 退出码不改变工具结果（仅非 0 警告）。**失败模式 = fail-open**（PRD D3）：hook spawn 失败 / 超时 → 警告 + 放行，不挂主循环（PermissionGuard 才是安全边界，hooks 是便利层）。JSON stdin payload：PreToolUse `{event,tool_name,tool_input,session_id,cwd}`；PostToolUse 追加 `{tool_output,is_error}`（字段名对齐 Claude Code）。集成：`agent_loop` 加可选 `hook_engine` 参数（PreToolUse 插在权限通过后 / handler 前，PostToolUse 插在 handler 成功后 / `_tool_result` 前，handler 异常不触发 PostToolUse）；session_id 经 `_resolve_hook_session_id` 复用 `compact_fn.get_session_id`，cwd 用 `os.getcwd()`。**子代理不传 `hook_engine`（隔离，hooks 只在主循环触发）**。`engine.py` 不 import `src.core.loop`（避免循环依赖，engine 被 loop 调用）。MVP 不支持改写 tool_input、不支持 Stop/Notification 等其余事件、不支持 env 注入 / 热重载（均为后续扩展位）。配置见 `config.toml [[hooks]]`。关键文件：`src/hooks/{__init__,events,config,engine,errors}.py`、`src/core/loop.py`（Pre/Post 插入 + `hook_engine` 参数）、`src/main.py`（`HooksConfig` + Config 字段 + 解析 + 建 engine 传主循环）。

### 配置热重载 (`src/main.py`，ROADMAP 4.3)
改完 `config.toml` / `config.local.toml` 后无需重启即可应用**可热重载**子集，并清晰区分哪些改动**需重启**。REPL 命令 `/reload`：`load_config(config.path)` 重读 toml+local+env（**不重穿 CLI provider/model 覆盖**——provider 本就需重启）→ `_diff_config_for_reload(old, new)` diff 并分类 → 应用 hot 子集到运行时对象 → 打印摘要（已应用 / 需重启 / 无变更）。**hot 集**（`_HOT_RELOAD_PATHS` frozenset）= `ui.theme` + `permission.{mode,allow,deny}`，改完即生效（theme 经 `get_theme().switch()` + `ui_console.set_theme()`，permission 直接原地改 `PermissionGuard.{mode,allow_rules,deny_rules}`）；其余字段（provider / mcp / lsp / hooks / retry / cost / debug / tracing / memory / subagent / thinking 等 boot 时固化的连接/客户端/烤进对象）仅报告「需重启」、**不**改运行时。**diff/分类为 main.py 模块级纯函数**（`Config` 定义在 main.py，独立 core 模块会造 core→main 循环依赖）：`_diff_config_for_reload` 用 `dataclasses.asdict` 拍平成 `section.field` dotted leaf（`_flatten_config` 下钻一层，`permission.allow`/`cost.prices` 等 list/dict 整体作单叶子比较，顺序变也算变；跳过 `path` resolved 路径字段），`_HOT_RELOAD_PATHS` 命中进 `hot` 否则进 `restart`，返回 `ReloadReport(hot, restart)`（`ConfigChange(path, old, new)` 列表 + `changed` 属性）。**失败安全 = all-or-nothing**（D4）：`load_config` 抛错（坏 TOML / 校验失败）→ `_dispatch_reload_command` 捕获 `Exception` + 报错 +「保持当前配置」，**零应用**，不留半套状态、进程不崩。apply 时**逐项 apply 并把 live `config` 对应子段原地 mutate**（hot 字段同步到 live config 使后续读一致，restart-required 字段保留 live 旧值——「正在运行的配置」与报告一致）；非法 theme 名 / 非法 mode 字符串 → 该项跳过不崩、其余 hot 项仍应用。**被动 mtime 监听**（D1，无后台线程——后台线程改 console/permission 会重蹈 Scheduler 线程安全坑）：`_config_mtimes(config)` 纯函数 best-effort stat 主文件 `config.path` + `.local` 兄弟文件（缺失文件跳过），REPL 主循环每轮读输入前比对 `last_config_mtimes`，变化则打印一行「config changed on disk — type /reload to apply」并更新记录值（同一 mtime 不重复刷；session 启动时初始化一次避免首轮误报）；`/reload` 后也刷新 `last_config_mtimes`。命令登记：`_SLASH_COMMANDS` + `_HELP_TEXT` + dispatch if 链（`if text == "/reload"`）。MVP 不支持后台 auto-watch+apply（Out of Scope，线程安全）、不引 watchdog（被动 mtime 零依赖）、不热重载 provider/mcp/lsp/hooks（boot 固化）。关键文件：`src/main.py`（`_diff_config_for_reload` + `ReloadReport`/`ConfigChange` + `_dispatch_reload_command` + `_config_mtimes` + mtime 检查 + dispatch 分支）、`config.toml`、`tests/test_config_reload.py`。

## 配置

`config.toml`（默认值）→ `config.local.toml`（本地覆盖，已 git-ignore）→ 环境变量 / CLI 参数（优先级递增）。

关键环境变量：`BAREAGENT_CONFIG`、`BAREAGENT_PROVIDER`、`BAREAGENT_MODEL`、`BAREAGENT_API_KEY`、`BAREAGENT_API_KEY_ENV`、`BAREAGENT_BASE_URL`、`BAREAGENT_PERMISSION_MODE`、`BAREAGENT_UI_STREAM`、`BAREAGENT_UI_THEME`、`BAREAGENT_THINKING_MODE`、`BAREAGENT_THINKING_BUDGET_TOKENS`、`BAREAGENT_SKILLS_DIR`、`BAREAGENT_SUBAGENT_MAX_DEPTH`、`BAREAGENT_SUBAGENT_DEFAULT_TYPE`。追踪相关：`LANGFUSE_PUBLIC_KEY`、`OTEL_EXPORTER_OTLP_ENDPOINT`。

CLI 参数：`--provider`、`--model`、`--config`。

## 代码规范

- 优先使用 Python 3.12+ 特性和标准库
- 遵循 PEP 8，保持清晰的类型注解
- 提交信息遵循 Conventional Commits：`Fix:`、`Feat:`、`Refactor:`、`Test:`、`Docs:`
- 新增行为需在 `tests/` 中补充 pytest 测试
- 保持实现简洁，避免过度设计

详细工程约定见 `.trellis/spec/backend/`（trellis 自动注入到所有 sub-agent 上下文）。
